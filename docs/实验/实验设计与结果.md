# 实验设计与结果

> 本文是「周期性业务履约的状态管理与异常恢复」的**验证层**文档：用可重复的并发实验，验证
> 状态迁移、幂等、资源一致性、异常恢复这几套机制是否真的成立，并记录实验过程中暴露的缺陷与修复。
>
> 配套文档：`实验环境与运行说明.md`（如何搭建与运行）、`../基线文档/业务过程与可靠执行模型.md`（机制设计）。

## 0. 实验总览

| 编号 | 实验名 | 验证命题 | 对照设计 | 结论 |
| --- | --- | --- | --- | --- |
| 实验一 | 有限资源并发扣减 | 并发扣减不会超卖，且资源被完整消耗 | 无保护「先查后减」 vs 有条件更新的行锁扣减 | 无保护超卖 100 盒；有保护零超卖、配额恰好用尽 |
| 实验二 | 重复支付回调 | 同一回调重复到达只产生一次有效副作用 | 无（同一机制内部一致性） | 20 次并发回调 → 1 次落账、1 条流水、1 条任务、1 次扣减 |
| 实验三 | 并发重复展开任务 | 并发重复生成不产生重复子任务 | 无保护「先查后插」 vs 数据库业务唯一键仲裁 | 16 线程并发生成 6 天任务 → 恰好 6 条，无重复 |
| 实验四 | 父子状态漂移的对账补偿 | 父状态与子过程不一致时能被检测并恢复，且补偿幂等 | 无（对异常恢复能力本身的验证） | 两类漂移均被修复到合法状态，重复对账不再产生迁移 |
| 实验六 | 补偿规则引擎 | 补偿规则由规则表驱动、且仍受迁移闸门约束 | 启用 vs 停用规则；闸门放开 vs 禁止 | 停用规则即不补偿；闸门禁止则不落地（补偿不绕过可靠性层） |
| 实验七 | 跨表不变量体检 | 跨表约束可被持续检出（退款域落地后新增 `INV_REFUND_AMOUNT`/`INV_REFUND_TERMINAL` 两项告警级检查）；可逆项自动修复、涉资金/终态/在途项仅告警；修复按声明依赖顺序执行 | 健康数据零误报 vs 注入 4 类违规；资金项；终态父过程下「待配送 vs 配送中」；终态订单网格缺口 | 4 类检出 4/修复 4、复检 0，订单未被误判、组合守卫零触发；资金项与在途任务只告警；终态网格缺口不补造历史 |
| 实验八 | 实时自愈与兜底双通道 | 实时通道把不可检测窗口从分钟级压到秒级；失败可退避与放弃 | 实时通道 vs 兜底通道周期；成功 vs 必然失败路径 | 实时窗口 ≈ 5.0 秒 vs 兜底 600 秒（缩短 119 倍）；超限放弃并转兜底 |
| 实验九 | 多实例部署的正确性 | 实例本地状态被消除；定时任务重复执行不产生重复效果；编号与实例解耦 | 两个组件实例只共享数据库；两个实例并发跑同一任务/批次/体检 | 跨实例确认扣款/查单/补偿全部打通；对账只落账一次；签收/体检不重复；任务号由业务键确定 |
| 实验十 | 混沌注入（事务中间故障） | 中间失败不留半截数据；被打断的业务由通道收敛 | 在扣配额/展开任务/签收事务内部注入异常；再叠加"混沌风暴" | 失败后状态/流水/任务/配额/台账全部回到原点；4 次注入全部由对账补回；风暴后零非法状态、零重复副作用 |
| 实验十一 | 长稳对账度量 | 混合负载下三通道持续收敛；检测延迟有界 | 业务负载 + 人为漂移 + 三通道并发，持续 6 秒（压缩负载） | 漂移 624 次全部收敛：兜底 543 / 体检 71 / 实时 10；检测延迟 p50 81ms、p95 216ms；最终零违规、零重复副作用 |
| 实验十二 | 性能基准（热点行争用） | 同一池子的扣减被行锁串行化；分散到多行吞吐回升 | 单池线程数扫描 1/8/32；32 线程下分散到 8/32 个池 | 热点随并发度**饱和**（8→32 线程吞吐 0.89×）而 p50 涨 9.8×；分散后吞吐 ×4；**全程零失败、零超卖、账实一致** |
| 实验十三 | 配送日平移与拒收补送 | 平移"合并/新建"只产生一次效果且可重试；补送按订单类型分叉（套餐合并、零散新建+追加配额）且先于完成判定；期末摊平保留补送量、作废窗口外任务且总量守恒 | 平移：合并（次日已有任务）vs 新建（周期外）vs 重复执行 vs 配送中预检；拒收：零散单日 vs 套餐多日；摊平：人为造"已有补送"后摊平 | 重复执行不重复加量（CAS 即幂等闸门）；零散新建+配额 1、订单保持配送中；套餐合并且配额台账 0 行；摊平 d2 = 计划 2 + 补送 1、窗口总量恒为 7、重复执行 0 条 —— **并暴露 D7** |
| 实验十四 | 周末停送与调休例外（日历重排） | 按日历自动重排（周末→周四/周五、停送日→前一工作日、补课日不重排）后，盒数守恒、幂等、历史不可改、单日≤3 盒、开关关闭时不生效 | 周末并入 vs 补课日跳过 vs 停送日提前；重复执行；**首选目标日已满 3 盒**时前推；**开关关闭** | 在途盒数 15→15 守恒；重复执行 shifted=0；周四保持 3 而前推到周三（2）；补课日 1/1 不重排；开关关闭抛 BusinessException |
| 实验十五 | 退款与毕业清算 | 并发申请只落一张进行中退款单；重复执行退款只落账一次；部分配送后退款金额守恒（含尾差）；退款与配送并发时金额按实际作废集合结算；缺货取消期次可退且配额不重复回补；毕业清算幂等 | 8 线程并发申请；6 线程并发执行；3 天套餐造"10 元除不尽"的尾差；退款与开始配送零栅栏并发；缺货取消后对**已完成(4)**订单退款 | 并发申请 1 成功/7 被唯一键挡回；并发执行仅 1 次落账、5 条任务只作废一次、台账 1 条；退 2 盒 = 6.67 且 6.67+3.33 = 10.00；竞态下作废 2 盒 + 已送出 2 盒 = 合同 4 盒、金额 6.00；缺货取消期次退 2 盒/6.00 元且配额 0 不减成负数；清算第二轮 0 单 |
| 退款契约 | 退款域 HTTP 契约（接口层） | 前端调用的路径/HTTP 方法/请求报文/响应字段与后端一致；未登录 401、班主任与配送站 403、家长仅限本人绑定学生 | 无（接口层断言；家长 token 由 `JwtTokenProvider` 直接签发，等价小程序登录态） | 管理员全链路 200（预览 3 盒/9.00 → 申请 → 审核 → 执行 → 可退归零、重复执行被拒）；TEACHER/DELIVERY 列表/执行/清算 403；家长他人订单业务码 403、审核与清算 HTTP 403 |
| 实验二十 | 仓库余量台账与配额发行封顶 | 到货/送出/退回/修正混合负载后余量恒等式成立；并发送出每任务恰一条 OUT；仓库余量封顶配额发行；短交预警不阻断登记；拒收→回仓→补送首尾相接；并发登记唯一键仲裁；出库不变量判据用 `dispatch_time`（1→4 未送出任务不得被判成漏记） | 有货 vs 无货设配额；删 OUT 行 vs 1→4 未送出任务（判据对照）；16 线程并发送出 / 8 线程并发登记 | W 恒等式两算一致（1,000,106）；16 线程并发 dispatched=4、OUT 恰 4 行；W=50 设 100 被拒、补货 60 后放行；应到 100 实到 80 → 预警且登记成功；拒收后余量回补、补送出库（含**单条路径**钩子）；并发登记 1 成功/7 被唯一键挡回；漏记 OUT 自动补写而 1→4 任务零误判 |

> 实验十六~十九（批次追溯 / 未送达申报 / 家长端当日豁免 / 过敏警示）的实测见 §6.13–§6.16。
| 契约矩阵 | 状态机契约不回归 | 规则覆盖完整、安全关键迁移被禁止、台账可回放、不变量执行顺序由声明依赖决定 | 无（结构性断言） | 50 个可达组合零缺失（新增退款单 AUDIT/REJECT/EXECUTE × 5 状态）；安全关键迁移均禁止（含新增「未审核不得执行/已退款不得重复执行」等 6 项断言）；台账回放 1→2→3→4；执行顺序 = 网格补全→父聚合→组合守卫（与字典序不同） |
| 补充用例 | 状态迁移留痕完整性 | 「每一次状态迁移都写入台账」这一不变量在所有路径上成立 | 无（不变量检查，非并发实验） | 跨三张表的每一步迁移都恰有一条生效台账；详见第 7.1 节 |

实验落在 `src/test/java/com/milk/order/experiment/`，契约矩阵落在
`src/test/java/com/milk/order/contract/`，可一次性重复执行；运行方式见 `实验环境与运行说明.md`。

## 1. 实验环境与方法

### 1.1 环境

| 项 | 值 |
| --- | --- |
| 数据库 | MariaDB 12.1.2（InnoDB），库 `student_milk_order_test` |
| 应用 | Spring Boot 2.7.18 / Java 17 / MyBatis-Plus 3.5.3.1 |
| 连接池 | HikariCP，`maximum-pool-size=64` |
| 事务隔离级别 | `READ_COMMITTED`（见第 7 节缺陷 D3） |
| 定时任务 | 全部关闭（`app.scheduler.enabled=false`），保证实验状态只由实验自身驱动 |

> 实验刻意同时覆盖 **MySQL 与 MariaDB 的行为差异**。项目文档以 MySQL 8 为目标，而实际运行环境是
> MariaDB；两者在可重复读下的语义差异本身成为实验发现之一（缺陷 D3）。

### 1.2 方法

1. **独立实验库**：实验只写 `student_milk_order_test`，不触碰开发库。库结构与开发库一致，
   并复制了 `state_transition_rule`（35 条迁移规则）、`process_reconcile_rule`（3 条补偿规则）
   与 `sys_config` 种子（状态机是白名单语义，缺规则会让所有迁移被拒）。
2. **夹具与清理**：`@BeforeEach` 建夹具（年级/班级/学生/品类/奶品），`@AfterEach` 清空业务表，
   因此实验可反复运行且结果不受历史数据影响。
3. **真实并发**：所有并发实验用固定线程池 + 双栅栏（`CountDownLatch` 就绪栅栏 + 放行栅栏），
   先把全部线程压到同一起点再同时放行，避免「先启动的线程已跑完」导致并发度虚高。
4. **数据存档**：每个实验的数据块同时打印到控制台并写入
   `target/experiment-reports/<实验类名>.txt`（UTF-8），便于复核与引用。

### 1.3 判定口径

- **承诺售出**：系统对用户返回成功的次数（用户视角「买到了」）。
- **实际消耗**：资源（配额/库存）真实被扣减的数量。
- 二者不相等即出现资源违背：承诺 > 消耗 = 超卖；承诺 < 消耗 = 少卖/泄漏。

## 2. 实验一：有限资源并发扣减（超卖）

### 2.1 验证命题

某日某品种机动配额 100 盒，200 个请求同时各买 1 盒：

- 有保护版本：成功数与配额实际消耗都应恰好等于 100，不超卖；
- 无保护版本：应能复现超卖，作为对照基线。

### 2.2 实验设计

**对照 A（无保护）**：模拟最朴素的实现——先读 `used_quota`，判断 `used+1<=total`，再写回 `used+1`。
为确定性地复现「丢失更新」，用一个 `CyclicBarrier(200)` 把所有线程卡在**已读到旧值之后、写回之前**，
使 200 个线程读到同一个初始值后再各自覆盖写回。

**对照 B（有保护）**：走 `DailyQuotaServiceImpl.deduct`，其并发语义为：

1. 事务内用 `SELECT ... FOR UPDATE` 加行锁读取保质期窗口内的配额池（先过期先出），一次性算出
   可扣总量并形成扣减计划；
2. 全部品种充足才继续，任一不足即整体抛异常（事务回滚，不留部分扣减）；
3. 按计划写回，并以 `used_quota + n <= total_quota` 作为条件更新兜底；
4. 每次扣减写 `daily_quota_usage` 台账，业务唯一键 `(order_id, product_id, quota_date)` 保证同一订单
   同一池子只扣一次。

用 200 个互不相同的订单号发起并发扣减（`FAKE_ORDER_BASE + i`）。

### 2.3 实测数据

**对照 A（无保护）**

```text
配额总量                100
并发请求数              200
系统承诺售出（成功率）    200
配额实际消耗            1
丢失更新次数            199
超卖盒数                100
```

**对照 B（有保护）**

```text
配额总量                100
并发请求数              200
扣减成功数              100
扣减失败数              100
配额实际消耗            100
超卖盒数                0
失败原因分布            100 × BusinessException: 「×××奶」2026-09-20 剩余库存 0 盒，不足本次订购的 1 盒，卖完即止
```

### 2.4 结论

- 无保护实现下，200 个请求全部被系统「承诺售出」，而配额只被消耗了 1 盒：**超卖 100 盒**
  （199 次写入被互相覆盖，即经典的丢失更新）。
- 有保护实现下，成功数恰好等于配额总量 100，配额被完整消耗且**零超卖**；失败的 100 次都拿到了
  明确的「剩余库存 0 盒」业务提示，而不是静默失败或数据错乱。
- 结论：**「加锁读 + 条件更新 + 台账唯一键」三者组合能把并发扣减收敛到确定结果**；
  单独使用条件更新（不加锁读）在 MariaDB 上会失败，见缺陷 D3。

### 2.5 实验暴露的缺陷

实验一在首次运行时**有保护版本也失败了**（成功数仅 25），失败原因是数据库异常
`Record has changed since last read in table 'daily_quota'`。根因与修复见第 7 节 D3。

## 3. 实验二：重复支付回调的幂等落账

### 3.1 验证命题

模拟微信在回调超时/抖动下重复投递同一笔支付成功通知。重复回调**不得**产生重复副作用：

1. 订单只发生一次「待支付 → 已支付」迁移；
2. 支付成功流水只有一条；
3. 迁移台账中该订单 PAY 的「已生效」记录只有一条；
4. 配送任务只展开一次；
5. 配额只扣减一次。

### 3.2 实验设计

1. 构造 1 个待支付订单（单日散订，2 盒）；
2. 组装一次成功回调报文（订单号 / 流水号 / 金额 / 支付时间 / `resultCode=SUCCESS`）；
3. 用 20 个线程**同时**投递这同一份报文（`handleWechatPayNotify`）；
4. 统计订单状态、流水条数、迁移台账条数、任务条数、配额扣减量。

### 3.3 实测数据

```text
回调投递次数                 20
回调处理成功（含幂等快速路径）   1
回调处理失败（CAS 冲突）       19
订单最终状态                 2（已支付）
支付成功流水条数              1
PAY 迁移生效条数              1
PAY 迁移冲突条数              0
配送任务条数（理论 1）          1
配额扣减盒数（理论 2）          2
失败原因分布                 19 × BusinessException: 订单状态已变更，回调处理冲突
```

### 3.4 结论

- 20 次并发回调只产生**一次**有效落账：1 条订单迁移、1 条支付流水、1 条配送任务、1 次配额扣减，
  其余 19 次都以明确的业务冲突提示结束。
- 三次迁移台账中「迁移生效」只有 1 条，说明过程层的迁移留痕与真实状态变更一致。

### 3.5 两条重要说明

**（1）为什么「迁移冲突条数」是 0？**
`ProcessTransitionExecutor.require` 在 CAS 失败时会写一条 `result=0` 的冲突台账，但随后抛出业务异常，
**同一事务整体回滚**，这条冲突台账也随之回滚。因此：
- 在「失败即中止事务」的严格路径（本实验）里，只有生效记录留痕；
- 在「失败即跳过、事务继续」的宽松路径（定时兜底、批量取消，用 `attempt`）里，冲突记录才会持久化。

这是刻意设计：台账记录的是「已提交的业务事实」，而不是内存里的尝试次数。

**（2）这个实验暴露了缺陷 D2 与 D4。** 首次运行时有 19 次回调的失败原因是「状态机规则禁止」，
而不是 CAS 冲突——根因是状态机规则缓存用了非原子的 `clear()+putAll()` 刷新（缺陷 D2）。
修掉缓存后又暴露：副作用（扣配额、写流水）发生在状态抢占之前，落败方已做副作用才失败，
并发下互相撞业务唯一键（缺陷 D4）。两处修复见第 7 节。

## 4. 实验三：并发重复展开配送任务

### 4.1 验证命题

一个覆盖 6 天、1 个品种的订单，理论应生成 6 条配送任务。并发重复展开（模拟「支付回调重复投递 +
手工补生成 + 定时展开」三者撞车）时，任务数量必须恒等于理论数量，不能出现 12 条、18 条这样的重复。

### 4.2 实验设计

1. 构造覆盖 6 天的订单（先建单日订单，再延后 `delivery_end_date`，绕过散订的「起止同日」校验）；
2. 16 个线程同时调用 `generateTasksForOrder(order)`；
3. 统计任务条数、签收记录条数、任务号去重条数、业务键组合数。

原始实现的幂等靠「先 `selectCount` 查是否已生成，再 `insert`」。该方式只在单线程下成立：并发下多个
执行者会同时查不到、再同时插入。改造后由数据库业务唯一键
`uk_order_product_date(order_id, product_id, delivery_date)` 仲裁，重复插入得到唯一键冲突并被幂等跳过。

### 4.3 实测数据

```text
并发调用次数                 16
本轮实际插入生效的线程数       1
未插入任何行的线程数（幂等跳过）  15
理论任务数                   6
实际任务条数                 6
签收记录条数（应与任务一一对应）  6
任务号去重后条数              6
业务键 (订单,品种,日期) 组合数   6
失败原因分布                 （无失败）
```

### 4.4 结论

- 16 个线程并发展开，只有 1 个线程真正插入了数据，其余 15 个全部命中唯一键冲突并被幂等跳过；
- 任务数、签收记录数、任务号去重数、业务键组合数**四者一致且等于理论值 6**，
  说明任务与签收记录严格一一对应，没有孤儿记录、没有重复任务；
- 结论：**把并发仲裁交给数据库唯一约束，比应用层「先查后插」可靠**。

## 5. 实验四：父子状态漂移的对账补偿

### 5.1 验证命题

正常路径下父订单状态由子过程驱动：

- 子任务开始配送 → 父订单进入「配送中」；
- 子任务全部到达终态 → 父订单聚合为「已完成」。

这两次推进都是普通数据库写入，一旦进程中断或异常，父状态会与子任务集合不一致，系统进入
「自洽但错误」的状态。本实验人为制造这两类漂移，验证系统能「发现不一致 → 补偿 → 回到合法状态」，
并且补偿本身幂等。

### 5.2 实验设计

1. 正常支付一个订单（订单 → 已支付，展开 1 条任务）；
2. **漂移一**：直接改库把子任务置为「配送中」，父订单仍停在「已支付」（模拟配送联动写入丢失）；
   运行 `ProcessReconcileCoordinator.reconcile(ORDER, limit)`；
3. **漂移二**：把子任务与签收记录直接置为终态，父订单仍停在「配送中」（模拟聚合写入丢失）；
   再次运行补偿；
4. **幂等验证**：状态已合法后再跑一轮补偿，统计「已生效的订单迁移条数」与「本轮探测到的漂移数」是否变化。

> 说明：补偿规则已下沉到 `process_reconcile_rule` 规则表（见实验六），本实验验证的是
> 「探测器 → 规则表决策 → 统一迁移出口」这条链路的端到端行为。

### 5.3 实测数据

```text
漂移一 · 补偿前订单状态        2（已支付）
漂移一 · 命中规则             联动丢失补偿
漂移一 · 补偿后订单状态        3（配送中）
漂移一 · 补偿生效数           1
漂移二 · 补偿前订单状态        3（配送中）
漂移二 · 命中规则             聚合丢失补偿
漂移二 · 补偿后订单状态        4（已完成）
漂移二 · 补偿生效数           1
补偿生效的订单迁移总条数        3
重复对账后再统计（应不变）      3
第三轮探测到的漂移数（应为 0）    0
```

### 5.4 结论

- 两类父状态漂移都能被对账任务发现并补偿回合法状态（2→3、3→4）；
- 补偿复用统一迁移出口与聚合出口，因此**天然幂等**：重复对账后「已生效迁移条数」不变（3 → 3），
  不会产生重复状态迁移或额外副作用；
- 结论：系统不依赖「每一步都成功」，而是具备「可检测、可恢复的最终一致性」。

## 6. 异常恢复的深化实验（实验六 ~ 八）

> 实验一~四验证了「资源一致性、过程幂等、异常恢复」的基本正确性。本组实验针对随后暴露出的
> **结构性偏薄**之处：补偿规则写死在代码里、漂移检测只能靠定时全表扫、跨表不变量从未在运行期被持续验证、
> **多实例部署从未被验证过**（而系统里藏着实例本地状态）、只在"完整失败或完整成功"的假设下验证过
> （没人验证"事务做到一半失败"）、以及**性能从未被测过**（而"一致性优先"是有代价的，代价必须被量出来）。
>
> 七组实验都带对照（启用/停用、健康/注入、实时/兜底、跨实例/并发重复执行、事务中间故障/风暴、
> 热点单池/分散多池），而不是只展示「能跑通」。

### 6.1 实验六：补偿规则引擎（规则驱动 + 迁移闸门）

**验证命题**

1. 漂移探测是**只读**的：调用探测不会改变任何业务状态；
2. 补偿由**规则表**驱动：停用规则后，同样的漂移不再被补偿；
3. 补偿仍受**迁移闸门**约束：闸门禁止时，即使命中规则也不落地。

**实验设计**

1. 制造漂移（子任务置为配送中，父订单仍为已支付）；
2. 只调用 `detect`（不执行），记录命中规则与订单状态；
3. 调用 `reconcile` 执行补偿，记录状态；
4. 把订单状态改回避分点，`UPDATE process_reconcile_rule SET enabled=0` 停用「联动丢失补偿」，
   再探测 + 执行一次，记录结果；随后恢复启用；
5. 用 `StateMachineService.updateRule` 把 `ORDER/DELIVER/2` 置为禁止（缓存立即刷新），
   再执行一次补偿，记录结果；随后恢复。

**实测数据**（`target/experiment-reports/Experiment6ReconcileRuleEngineTest.txt`）

```text
探测命中规则                    联动丢失补偿
命中子过程条件                  HAS_DISPATCHING_TASK
只探测不执行的订单状态            2（应仍为 2）
执行补偿后订单状态               3
停用规则后探测到的漂移数          0
停用规则并执行补偿后的订单状态      2（应仍为 2）
恢复规则后探测到的漂移数          1
闸门禁止时本轮补偿生效数          0
闸门禁止时的订单状态             2（应仍为 2）
闸门恢复后订单状态               3
```

**结论**

- 探测确实只读（第 1 条断言）；
- 补偿确实由数据驱动：规则停用后探测器**不再命中**，补偿自然不发生；
- **补偿不是绕过可靠性层的旁路**：闸门禁止时命中规则但补偿生效数为 0，
  这也是「配了规则 ≠ 配对了规则」的直接证据——启用一条新补偿规则时，必须同步检查
  `state_transition_rule` 是否放开了对应的迁移（种子数据里第三条示例规则就带着这条注释）。

### 6.2 实验七：跨表不变量体检

**验证命题**

1. 体检在健康数据上**零误报**；
2. 可自动修复的 4 类不变量：能被检出、能被真正修好（数据一致而非只标记记录）、
   且在下一轮体检中**复检闭环**；
3. 涉资金的不变量**只告警**：被检出、记录为未闭环、且不改动任何数据；
4. 修复动作之间的**执行顺序**按声明依赖执行，体检不会自己制造组合异常（§2.5 方案二的行为验证）。

**实验设计**

对照 A：构造 3 天订单，前两日送出并签收、第三日留作待配送（订单处于**配送中**的进行中状态），
先跑一次体检证明零误报；然后**直接改库**注入 4 类违规（不变量要防的本就是绕过业务出口的旁路写入）：

| 注入 | 手段 | 对应不变量 |
| --- | --- | --- |
| 配额账实不符 | `daily_quota.used_quota += 3` | `INV_QUOTA_LEDGER` |
| 任务已完成但记录未签收 | `delivery_record.sign_status = 2` | `INV_TASK_RECORD` |
| 已签收但营养记录丢失 | `DELETE nutrition_intake` | `INV_SIGN_INTAKE` |
| 任务网格缺一天 | `DELETE delivery_task` + 记录 | `INV_ORDER_TASK` |

> 用**进行中**订单而不是已完成订单：注入后、补网格前，三个任务里两个已是终态、第三个被删除，
> 因此若「父聚合」排在「网格补全」之前，**同一次体检**就会先把订单判成已完成（3→4）、
> 随后网格补全又补回一条待配送任务——体检自己制造出组合异常。声明依赖后顺序为
> 网格补全 → 父聚合 → 组合守卫，两者都不会发生。这是方案二的行为验证。

对照 B：制造父状态漂移（子任务已送出、订单仍为已支付）**同时**删除成功流水，
跑两轮体检，观察「自动修复」与「仅告警」两条分界线。

对照 C：构造一个已完成订单，再直接用改库把它的子任务分别置为「配送中」与「待配送」，
各跑一轮体检，验证**同一条不变量内部的等级细分**——配送中（奶已出库在途）只能告警、
待配送（奶未出库）可安全自动作废，且作废经业务取消出口执行（留痕、联动作废签收记录）。

对照 D：把已完成订单的整条任务与签收记录删掉（模拟历史被改过），验证网格缺口
**只告警、不补造历史**——因此也不会给已结束的订单造出待配送任务（即不再制造组合异常）。

**实测数据**

对照 A（`Experiment7InvariantScanTest.txt`）：

```text
健康状态下检出数（应 0）                               0
注入前订单状态                                  3（配送中）
注入违规数                                     4
首轮检出总数                                    4
首轮自动修复数                                   4
INV_QUOTA_LEDGER 检出/修复                    1 / 1
INV_TASK_RECORD 检出/修复                     1 / 1
INV_SIGN_INTAKE 检出/修复                     1 / 1
INV_ORDER_TASK 检出/修复                      1 / 1
INV_PARENT_TERMINAL_CHILD_PENDING 检出（应 0）   0
次轮检出总数（复检闭环）                            0
修复后订单状态（应仍为 3，未被误判为已完成）                3
修复后任务数（应 3）                              3
修复后待配送任务数（应 1，即被补回的第三日）               1
修复后营养记录数（应 2，第三日未签收故无摄入）             2
```

对照 B：

```text
INV_ORDER_AGGREGATION 检出/修复    1 / 1
INV_PAYMENT_CONSISTENCY 检出/修复  1 / 0
补偿后订单状态                   3（配送中）
次轮 INV_ORDER_AGGREGATION 检出    0
次轮 INV_PAYMENT_CONSISTENCY 检出  1
资金告警记录状态（0=未闭环）         0
资金告警记录等级                  ALERT_ONLY
自动修复后剩余支付流水数（应 0）      0
```

对照 C（终态父过程下残余子任务的分级处置）：

```text
订单终态                              4（已完成）
情形一 · 残余任务状态（配送中）              2
情形一 · 守卫检出/修复                  1 / 0
情形一 · 降级为仅告警条数                 1
情形一 · 体检后任务状态（应仍为 2，未被作废）     2
情形一 · 体检记录状态/等级                0 / ALERT_ONLY
情形一 · 违规明细  父订单 EXP…（4 已完成）仍存在配送中的子任务（配送日期 2026-09-20）
情形二 · 残余任务状态（待配送）              1
情形二 · 守卫检出/修复                  1 / 1
情形二 · 体检后任务状态（应 4=已取消）        4
情形二 · 签收记录状态（已签收保持 1，不被作废联动覆盖） 1
情形二 · 体检记录状态/等级（应 1 已闭环/AUTO_REPAIR） 1 / AUTO_REPAIR
情形二 · 修复产生的 TASK_CANCEL 生效台账条数   1
```

对照 D（终态订单网格缺口只告警不补造）：

```text
订单状态                              4（已完成）
INV_ORDER_TASK 检出/修复              1 / 0
降级为仅告警条数                        1
体检后订单任务数（应 0：没有补造历史）          0
体检后订单状态（应仍为 4）                  4
INV_PARENT_TERMINAL_CHILD_PENDING 检出（应 0：未制造组合异常） 0
体检记录状态/等级（应 0 未闭环 / ALERT_ONLY）  0 / ALERT_ONLY
违规明细      订单状态 4（已完成），期望任务网格：1 个（品种×配送日）组合；实际：0 个，缺失 1 个
```

**结论**

- 健康数据零误报，说明检查口径（尤其是台账求和必须限定 `deleted = 0`）是可用的；
- 对照 A 的 4 类可自动修复项全部「检出→修复→复检闭环」，且任务网格在库中被真正补齐；
  更关键的是**订单始终停在配送中**、组合守卫零触发——若不声明依赖，
  同一次体检就会先把订单误判为已完成、再补回任务，**体检自己制造组合异常**；
- 资金不一致被检出但**没有被动过一个字节**，记录保持未闭环、等级为 `ALERT_ONLY`，
  次轮仍然报出——这条对照就是「哪些该自动修、哪些不该」的分界线的实证；
- 对照 C 说明等级可以细到**每条违规**：同一条不变量下，待配送的残余任务被自动作废
  （任务→已取消、`TASK_CANCEL` 生效台账 1 条、记录闭环为 `AUTO_REPAIR`），
  配送中的残余任务保持原状、记录挂着 `ALERT_ONLY` 等人工。口径与「缺货批量取消只取消待配送任务」一致；
- 对照 D 说明"保守"的一侧：终态订单的网格缺口**只告警不补造**——
  **不为了把数字补整齐去编造历史**，这一点正是组合异常不再产生的根因。

**附带发现与其加固（诚实记录整条链路）**

对照 A 在最初实现时有一个观察项是「修复后未完成任务数 = 1」：各条修复都正确，但
**修复动作之间缺少依赖与顺序**——任务网格补齐（`INV_ORDER_TASK`）发生在父状态聚合
（`INV_ORDER_AGGREGATION`）之后，于是「已完成的订单」被补出了一个待配送任务。
**当时 6 条不变量都没有被违反**，因为没有任何一条覆盖「父过程已终态时不应存在非终态子过程」
这个组合约束。它说明：*单个机制正确 ≠ 机制组合正确*。

加固分两步（§2.5）：

1. **方案一 · 组合状态守卫** `INV_PARENT_TERMINAL_CHILD_PENDING`：把该状态变成可见、可收敛
   （当时实测守卫命中 1 条并同轮作废，未完成任务数归零）；
2. **方案二 · 声明依赖 + 拓扑序 + 根因收窄**：给 `ProcessInvariant` 增加 `dependsOn()`，
   体检器按拓扑序执行（网格补全 → 父聚合 → 组合守卫）；同时让 `INV_ORDER_TASK`
   按订单状态分级——进行中补网格、**终态只告警不补造历史**。
   于是"先补上、再作废"的绕圈过程消失，组合异常**根本不产生**，
   组合守卫退化为最后防线（只在人工改库、进程异常等真异常下才命中）。

**仍在展望里的部分（方案三）**：不变量之间的相互影响目前靠**逐对声明**依赖来消解，
本质仍是"人把关系写出来"。更远的形态是把不变量看作**约束系统**，
由约束集合推导出一个满足全部约束的修复计划（而不是逐条独立修复 + 依赖排序），
见 `../论文/后续扩展方案.md` §2.5。

### 6.3 实验八：实时自愈通道与兜底通道

**验证命题**

1. 实时通道能在「轮询间隔 + 修复耗时」量级内收敛，显著优于兜底通道的调度周期；
2. 待办按业务键去重，重复触发不会把待办表冲爆；
3. 实时通道失败时会**指数退避**，超上限后**放弃并转交兜底通道**（这正是不需要放弃实时通道的理由）。

**实验设计**

- 对照 A：制造漂移 → 入队待办 → 领取（`claimDue`）→ `reconcileEntity` → 标记完成，
  测量各步耗时，并与两个通道的调度周期对比。刻意说明：本实验直接调用消费者使用的那组方法，
  与 `ProcessPendingTaskJob` 的循环体是同一组调用，因此测得耗时真实，端到端只多一个轮询间隔；
- 对照 B：同一业务键连续入队 10 次，统计待办行数；再入队一个不同触发动作，再统计；
- 对照 C：入队一条**必然失败**的待办（未注册场景）→ 处理失败 → 观察退避期内不可再领取 →
  驱动到重试上限 → 观察状态变为「已放弃」并记录失败原因。

**实测数据**

对照 A / B / C（`Experiment8RealtimeSelfHealingTest.txt`）：

```text
【对照A】待办入队耗时(ms)                    2
         待办领取 + 补偿 + 标记完成耗时(ms)      14
         命中的补偿规则                        联动丢失补偿
         补偿后订单状态                        3（配送中）
         待办终态（1=已处理）                   1
         实时通道检测窗口上界(ms)                 5014（轮询 5000 + 修复）
         兜底通道检测窗口上界(ms)                 600000
         窗口缩短倍数                          119
【对照B】同一触发动作入队 10 次后的待办数          1
         再入队另一触发动作后的待办数             2
【对照C】首次失败后是否进入退避（退避期可领取数）     0
         尝试次数                            5
         重试上限（与生产策略同源）              5
         最终状态（2=已放弃）                   2
         最终重试次数                          5
         最近一次失败原因                       未注册场景 UNKNOWN_SCENE 的父过程探测器
```

**结论**

- 实时通道把**不可检测窗口**从兜底通道的 600 秒压到约 5.0 秒（119 倍），
  且修复本身只有十几毫秒——瓶颈是轮询间隔而不是修复；这说明「实时」的收益来自
  「发现得早」，而不是「修得快」；
- 去重键含触发动作：10 次重复入队只留 1 行待办（批量送出 78 条任务同理），
  不同触发动作各留一行；
- 失败路径完整：退避期内不可再领取 → 重试 5 次 → 状态置「已放弃」并记录原因。
  **这正是双通道不可省略的理由**：实时通道自己也会失败，必须有兜底通道兜住。

### 6.4 实验九：多实例部署的正确性

**验证命题**

多实例下出问题的从来不是算法，而是两类东西：**实例本地状态**（别的实例看不见）与
**幂等边界**（同一件事被两个实例各做一次）。五个对照分别验证：

1. 支付状态不再依赖实例内存：跨实例确认扣款、跨实例查单、跨实例补偿落账都成立；
2. 两个实例并发跑同一个支付对账任务，只落账一次；
3. 两个实例并发跑同一个自动签收批次，每条记录只签收一次；
4. 两个实例并发体检，违规记录不重复、数据最终一致；
5. 编号生成不依赖实例本地计数器（订单号带实例标识、任务号由业务键确定）。

**实验设计**

方法学说明（重要）：在单 JVM 内用 **两个组件实例 + 并发线程** 模拟两个应用实例——
它们**只共享数据库**，各自持有自己的组件对象。这覆盖的是"共享数据库、各自持有组件"的多实例语义；
不覆盖真实多进程/多主机、网络分区与负载均衡路由（见「已知局限」）。

| 对照 | 构造 | 断言要点 |
| --- | --- | --- |
| A | 构造两个 `WechatPaySimulator` 实例：A 预下单 → B 确认扣款 → A 查单；再让回调丢失，走对账补偿 | B 受理成功、A 查得到扣款、补偿落账 1 次、订单已支付、成功流水/台账/任务各一份 |
| B | 两个线程同时执行 `reconcilePendingPayments`（= 两个实例的对账任务体） | 只有一个实例报告"补偿了订单"；订单/流水/台账/任务/配额台账各一份 |
| C | 两个线程同时执行自动签收批次（逐条 `autoSignOne`） | 记录只签收一次、营养摄入只一条、任务与订单终态、SIGN 台账一条 |
| D | 两个线程同时 `invariantScanner.scan(true, …)`（注入配额账实不符） | 同一主体的违规记录**只有一行**、`used_quota` 收敛到台账合计 |
| E | 走真实下单支付路径，检查订单号/流水号与任务号的构成 | 编号含实例标识；任务号等于确定性公式 `DT{日期}-{订单}-{品种}`，且与业务键一一对应 |

**实测数据**（`target/experiment-reports/Experiment9MultiInstanceTest.txt`）

对照 A（跨实例支付状态）：

```text
B 实例确认扣款是否受理（内存态下必为 false）        true
A 实例查单是否拿到扣款结果（内存态下必为 null）      已扣款 TX17898102142955951
微信侧支付单行数（预支付 / 已扣款）              1 / 1
回调丢失后对账补偿落账数                     1
落账后订单状态（2=已支付）                    2
成功支付流水条数 / PAY 生效台账条数 / 配送任务数     1 / 1 / 1
```

对照 B（两个实例并发支付对账）：

```text
报告"本轮补偿了订单"的实例数（应 1）        1
本轮未补偿的实例数（CAS 冲突，被任务内部消化）    1
订单状态（2=已支付）                   2
成功支付流水条数 / PAY 生效台账 / 配送任务 / 配额台账   1 / 1 / 1 / 1
```

对照 C（两个实例并发自动签收）：

```text
候选中标记录数                        1
签收记录状态（1=已签收） / 该记录营养摄入条数    1 / 1
任务状态（3=已完成） / 订单状态（4=已完成）     3 / 4
任务 SIGN 生效迁移台账条数（应 1）          1
```

对照 D（两个实例并发体检）：

```text
两个实例本轮都检出了违规                    2 / 0
同一主体的违规记录行数（应 1）                 1
修复后的 used_quota / 台账合计             1 / 1
体检记录最终状态（1=已闭环，观察项）             1
```

对照 E（编号生成）：

```text
订单号/流水号是否含实例标识（本次实例 = PFNOE）    true
任务号（第 1 天 / 第 2 天）                 DT20260920-32-54 / DT20260921-32-54
任务号去重条数 / 任务业务键组合数 / 一一对应       2 / 2 / true
```

**结论**

- **本地状态是真正的杀手**：支付状态放在内存里时，跨实例的"确认扣款"与"查单"必然失败；
  其中的"查单失败"尤其危险——对账任务会把已扣款订单看作未付款，
  进而可能被超时任务取消（**钱已收、单已取消**）。落到 `wechat_pay_order` 表后，
  跨实例确认/查单/补偿三条路径全部打通（对照 A）；
- **不选主也正确**：定时任务在 N 个实例上都会跑，但每个任务的写路径都是 CAS/唯一键/幂等跳过，
  因此"重复执行只浪费算力，不产生错误"（对照 B、C、D 各证明一项）；
- **编号必须与实例解耦**：原先"时间戳 + JVM 内自增"在多实例下会撞号。任务号改为**由业务键确定性生成**
  之后，"任务号冲突 ⟺ 业务键冲突"，顺带消除了一个隐藏缺陷——**原实现会把"任务号撞了"
  误判成"业务键已存在"而静默跳过，导致一条配送任务凭空消失**；订单号则加入实例标识（对照 E）；
- **并发写入靠唯一键收口**：两个实例同时体检不会插出两条违规记录（对照 D）。

**与审计表的关系**：完整的组件级审计（哪些有本地状态、后果、处置）写在
`../基线文档/可靠性设计.md` 第十节，本节只记录可执行的验证；
未覆盖的部分（真实多进程、网络分区、回调发送仍在实例内、60 秒缓存收敛、
`retry_count` 加速前进、时钟偏移等）同样列在该节 10.3。

### 6.5 实验十：混沌注入（事务中间故障）

**验证命题**

前九个实验验证的都是"完整的坏数据"（重复回调、漂移、并发冲突）。本实验验证**中间失败**：
事务执行到一半被打断时，① 会不会留下半截数据；② 被打断的业务能否被通道收敛。

**实验设计（含方法学说明）**

注入器是**测试域 AOP**（`src/test/java/com/milk/order/chaos/ChaosInjector`），默认撤防、固定种子（可复现），
在真实事务的中间步骤**执行完之后**抛异常——这精确对应「这一步的写入已发生、整个事务尚未提交」。

> **方法学要点（一个必须写清的约束）**：注入点必须位于**某个外层事务内部**。
> 因为注入语义是"先执行、再抛"，若注入点自身就是事务边界，`proceed` 已经提交，
> "抛异常"就变成另一种现象——**调用方看到失败、实际已生效**（模糊失败/悬挂），
> 那是另一个课题，本实验不混用。

| 对照 | 注入点（事务内部的哪一步） | 断言要点 |
| --- | --- | --- |
| A | 支付落账事务里的**扣配额** | 失败后：订单仍待支付、0 流水、0 任务、配额 0、台账 0、PAY 迁移台账 0；对账查单后补偿落账 1 次 |
| B | 支付事务的**最后一步：展开任务网格** | 此前所有写入（状态抢占、配额扣减、支付流水）一并回滚；撤防后重试完整成功 |
| C | 签收事务内的**父状态聚合** | 记录/任务/营养/父状态全部回滚；重试后一次性完成，且台账恰好 1 条 |
| D | 全部注入点，60% 概率、最多 4 次（"混沌风暴"） | 4 笔被打断的支付全部被对账补回；6 处人为漂移 2 轮收敛；最终零非法状态、零重复副作用、零不变量违规 |

**实测数据**（`target/experiment-reports/Experiment10ChaosInjectionTest.txt`）

对照 A（支付落账中途失败）：

```text
注入点                              DailyQuotaService.deduct（扣配额）
捕获到的异常                        ChaosInjectedException: 事务中间故障 —— DailyQuotaServiceImpl.deduct
失败后 · 订单状态（应 1=待支付）           1
失败后 · 成功流水/任务/配额/台账/PAY 台账     0 / 0 / 0 / 0 / 0
对账通道补偿落账数（应 1）               1
收敛后 · 订单状态（2=已支付）             2
收敛后 · 成功流水/任务/配额/台账/PAY 台账     1 / 1 / 1 / 1 / 1
收敛后 · 全库不变量检出数（应 0）           0
```

对照 B（任务展开中途失败）：

```text
失败后 · 订单状态（应 1=待支付）        1
失败后 · 配额已售数（应 0）            0
失败后 · 配额台账行数（应 0）           0
失败后 · 成功支付流水条数（应 0）         0
失败后 · 任务/签收记录条数（应 0 / 0）     0 / 0
重试后 · 订单状态（2=已支付）           2
重试后 · 配额已售数（应 1）            1
重试后 · 任务/签收记录条数（应 1 / 1）     1 / 1
重试后 · PAY 生效迁移台账条数（应 1）      1
```

对照 C（签收中途失败）：

```text
注入点                          OrderInfoService.completeOrderIfAllTasksDone（签收事务内的父状态聚合）
失败后 · 签收记录状态（应 2=未签收）      2
失败后 · 任务状态（应 2=配送中）         2
失败后 · 营养摄入条数（应 0）           0
失败后 · 订单状态（应 3=配送中）         3
重试后 · 签收记录/任务/营养/订单         1 / 3 / 1 / 4
重试后 · SIGN 生效迁移台账条数（应 1）    1
```

对照 D（混沌风暴）：

```text
订单数 / 注入次数 / 命中调用数       6 / 4 / 8
阶段一 · 因注入而失败的回调数         4
阶段二 · 对账通道补偿落账数           4
阶段三 · 人为制造漂移数 / 双通道补偿数   6 / 6（2 轮收敛）
最终 · 非合法状态订单数（应 0）        0
最终 · 重复成功流水 / 重复 PAY 台账    0 / 0
最终 · 全库不变量检出数 / 未闭环数      0 / 0
```

**结论**

- **事务边界是防"半截数据"的真正手段**：三次注入分别在支付事务的中间、末尾与签收事务的末尾，
  失败后库里的状态、流水、任务、配额、台账全部回到原点——**没有"改了一半"的中间态**；
- 这也解释了本项目为什么把"一次业务动作涉及的多张表写入"都放在同一事务，
  并把事务边界放在**外部调用入口**（缺陷 D6 的教训）；
- **被中断的业务由通道收敛**：4 笔被打断的支付全部由对账补回（对照 D），
  且补回后每笔仍然只有一份副作用（1 条成功流水、1 条 PAY 台账），说明补偿路径本身幂等。

### 6.6 实验十一：长稳对账度量

**验证命题**

把"业务写入 + 人为漂移 + 三通道并发收敛"压在同一段时间里持续进行，度量：
① 三条通道各自做了多少工作；② 检测延迟分布；③ 持续负载后是否仍然一致。

> **与"2~24 小时长稳"的关系（必须说清）**：真实长稳需要小时级运行，单测做不到。
> 本实验用**高密度压缩**：负载压在 6 秒内持续进行，**度量口径与真实长稳一致**
> （同一批 SQL 与同一组计数器），只是时间尺度被压缩。
> 因此它能回答"机制在持续负载下是否稳定收敛"，**不能**回答
> "运行 24 小时后有没有缓慢退化（缓存/表膨胀/连接泄漏）"——那仍属后续工作。

**实验设计**

```
业务线程 ×4        把 12 个订单推进到已完成（各自负责一部分）
漂移注入线程 ×1     持续把「已完成」订单回退为「配送中」（模拟聚合写入丢失，只注入规则表覆盖得到的漂移）
实时通道线程 ×1     drainPendingTasks（连续消费待办，等价于把 5 秒轮询间隔压到 10ms）
兜底通道线程 ×1     reconcile(ORDER, 500)
体检线程 ×1         invariantScanner.scan(true, 500)
         ↓ 停机 → 再做一次收敛 → 全库体检 → 度量
```

另含一个**特征化测试**记录发现的规则覆盖盲区（见下）。

**实测数据**（`target/experiment-reports/Experiment11LongRunMetricsTest.txt`）

```text
负载时长(ms) / 订单数                          6000 / 12
业务操作成功数 / 并发冲突数                      36 / 0
人为注入漂移次数                              624
实时通道 · 轮次 / 消费待办并补偿生效数             362 / 10
兜底通道 · 轮次 / 补偿生效数                    75 / 543
体检 · 轮次 / 自动修复生效数                    33 / 71
停机后最终收敛 · 补偿生效数 / 轮次                 0 / 1
迁移台账 · SIGN 生效条数 / CAS 冲突条数            24 / 9
待办 · 总行数 / 已处理 / 已放弃 / 到期未处理        27 / 24 / 0 / 0
体检记录 · 行数 / 重复漂移合计                    12 / 125
检测延迟(ms) · 样本数 / p50 / p95 / max         24 / 81.0 / 216.0 / 218.0
最终 · 已完成订单数（应 = 12）                   12
最终 · 非合法状态订单数（应 0）                   0
最终 · 重复成功流水 / 重复 PAY 台账的订单数         0 / 0
最终 · 营养摄入条数（应 = 12）                   12
最终 · 全库不变量检出数 / 未闭环数                 0 / 0
```

**结论**

- **持续负载下能稳定收敛**：624 次人为漂移全部被收掉（兜底 543 + 体检 71 + 实时 10），
  停机后只需 1 轮就无活可干，最终零违规、零重复副作用、零非法状态；
- **三条通道的分工在数据里很清楚**：
  - **兜底通道是主力**（543 次）：因为注入的是"父状态被改坏、但没有发生任何子过程事件"的漂移，
    **没有事件可驱动实时通道**；
  - **实时通道只做了 10 次**：它对应的是"子过程状态变更后父过程需要重新聚合"这一类，
    本实验里就是 12 次签收产生的待办（部分在消费时已无漂移）；
  - **体检做了 71 次**：与兜底通道同源（都覆盖父子一致性），两者互为冗余——
    这正是"一条不变量落地在多条通道上"的体现；
- **这条观察很重要**：事件驱动的实时通道**对"没有事件发生的写入丢失"无能为力**，
  所以它永远不能替代周期扫描；反过来周期扫描的检测窗口太长，所以也不能替代实时通道。
  双通道不是重复建设，而是覆盖两类不同的失效形态（**这条论证可以作为论文里的设计依据**）；
- **检测延迟有界**：待办从入队到处理完成，p50 81ms、p95 216ms、max 218ms；
  加上生产环境的 5 秒轮询间隔，即"实时通道最坏 5 秒级"（与实验八的口径一致）；
- 期间 9 次 CAS 冲突（多通道同时对同一订单补偿）全部被吸收：**冲突体现为"未生效"，而不是重复副作用**。

**同时发现一个规则覆盖盲区（当时记录，后已修复，见下）**

特征化测试构造了「订单已支付(2) + 子任务全部已签收(3)」这一状态（模拟配送联动与聚合写入同时丢失）：

```text
现有规则命中                            （无）
双通道补偿生效数（当时 0 = 未被覆盖）         0
通道跑完后的订单状态（当时仍为 2）            2
不变量体检检出数（当时 0 = 现有不变量也看不出）  0
```

原因：规则表里有 `(2, HAS_DISPATCHING_TASK → 3)` 与 `(3, ALL_TASKS_TERMINAL → 4)`，
**缺 `(2, ALL_TASKS_TERMINAL → 4)`**。

> **✅ 已修复（同日，3.4）**：新增补偿规则「已支付全终态补偿」`(2, ALL_TASKS_TERMINAL → AUTO_COMPLETE, 4)`，
> 并把迁移闸门 `ORDER/AUTO_COMPLETE/2` 从"显式禁止"改为"允许"（闸门看不见子过程，
> 安全性由聚合出口"只在全部任务终态时触发"的定义保证；管理端手动完成的 `COMPLETE/2` 仍保持禁止）。
> 该用例已翻转为回归守卫：命中规则、补偿生效 1 次、订单到已完成、体检零检出。
> 实现还顺带澄清了一个口径：**闸门该放哪一条**——最初误判为 `COMPLETE/2`（手动完成），
> 实际补偿走的是 `AUTO_COMPLETE`，两条闸门语义不同，放开哪条是独立的业务决定。

### 6.7 实验十二：性能基准（配额池热点行争用）

**验证命题**

本系统的扣减是「加行锁读剩余 → 写回已售」的读-改-写，因此**同一池子上的扣减会被行锁串行化**——
并发度再高，热点吞吐也不会线性扩展，而延迟会按并发度排队上升。这是"一致性优先"的直接代价，
必须被量出来，"单池 vs 分段池"的讨论才有依据。

**实验设计**

```text
预热 500ms（不计入指标，把 JIT 与连接池喂热）
A 组  热点单池：1 / 8 / 32 线程抢同一个池子
B 组  同为 32 线程，把配额分散到 8 / 32 个品种池（每线程绑定一个池，8 池时 4 线程共享一个）
每配置 1.5 秒；指标：TPS、p50/p95/p99/max（成功口径）、失败数、账实一致
```

> **口径与边界**：压的是**进程内服务调用**（`DailyQuotaService.deduct`），不含 HTTP 与序列化
> ——目的是单独暴露"行锁串行化"这一个因素；池容量给足，测吞吐而非失败路径（超卖拒绝已在实验一验证）；
> 本项目配额池唯一键是 `(quota_date, product_id)`，**没有"按班级分段"这一维**，
> 所以"分段池"用**多品种池**做等价代理（同为"把热点分散到多行"），真按班级分段需要改配额维度（后续工作）。

**实测数据**（`target/experiment-reports/Experiment12QuotaHotspotBenchmarkTest.txt`；
单机开发环境，多次运行波动约 ±20%，以下为其中一轮）

```text
场景                         [线程/池]   成功次数   TPS      p50     p95     p99    max(ms)
热点单池 · 1 线程             [1 / 1]      313    208.27    4.63    6.35    7.31    8.57
热点单池 · 8 线程             [8 / 1]     1146    758.55   10.29   13.81   15.65   19.55
热点单池 · 32 线程            [32 / 1]    1036    672.01   45.27   60.67   96.01   98.72
分散 8 池 · 32 线程           [32 / 8]    4067   2686.84   11.34   16.73   20.82   50.53
分散 32 池 · 32 线程          [32 / 32]   4028   2669.65   10.71   19.69   29.11   82.32

热点 32 线程 / 单线程 TPS：      3.23 ×（并发度 32× ⇒ 远非线性）
热点 8 → 32 线程吞吐增幅：      0.89 ×（并发度 4× ⇒ 已饱和，甚至略降）
热点 32 线程 p50 / 单线程 p50：  9.78 ×（并发度 32× ⇒ 排队而非变慢）
分散 32 池 / 热点 32 线程：      3.97 ×
分散 8 池 → 32 池吞吐增幅：      0.99 ×（收益递减：已接近锁外瓶颈）
全部配置失败数：0；账实一致：池已售合计 == 成功扣减数 == 台账行数，零超卖
```

**结论**

- **热点池确实被行锁串行化**：32 线程的吞吐（672 TPS）甚至**低于** 8 线程（758 TPS）——
  并发度提高 4 倍、吞吐反而下降，p50 却从 10.3ms 涨到 45.3ms（4.4×）、相对单线程涨 9.8×。
  **吞吐不涨、延迟线性涨**，这就是串行化瓶颈的标准特征；
- **分散到多行后吞吐回升约 4 倍**（672 → 2670 TPS），p50 回落到 10.7ms——
  同一份代码、同一份数据规模，唯一的差别是热点不在一行上；
- **两个修正了直觉的发现**：
  1. **"单线程 TPS = 吞吐上限"是错的**：单线程 TPS（208）是"单次端到端耗时的倒数"，
     它把连接获取、幂等查询、提交等**锁外开销**也算进去了；多线程可以把这些开销与锁内临界区
     **流水重叠**，所以热点并发吞吐（672）高于单线程 TPS（约 3.2×），但仍远低于 32× 的线性——
     临界区吞吐 ≈ 670 ops/s（≈1.5ms/次），这才是该行锁的真实服务能力；
  2. **分散的收益在 8 个池就到顶**（8 池 → 32 池只有 0.99×）：说明行锁之外还有第二个瓶颈
     （每笔扣减要执行 3 次保质期窗口加锁读 + 1 次条件更新 + 1 次台账插入，共 5 条 SQL，
     再加连接与 ORM 开销）。**要再上一个量级，得减少每笔扣减的 SQL 次数**（例如先做一次范围读
     确定窗口内实际存在的池，再逐池加锁），而不是继续分散；
- **正确性在压力下没有松动**：五个配置共约 10600 次并发扣减，全部成功、零失败、零超卖，
  且每个配置结束后"池已售合计 == 成功扣减数 == 台账行数"——**性能实验同样断言不变量**；
- **"按班级分段"的代价（推论，未实测）**：分段能解决热点，但不同班级不能互相借用余量，
  会降低配额利用率（某班卖完另一班还有货）。所以这是**一致性-吞吐-利用率**的三方权衡，
  而不是"分段一定更好"。

### 6.8 契约矩阵测试（结构性回归守卫）

**验证命题**：状态机的三条结构性约束在任何改动后都不能被悄悄破坏。

| 矩阵 | 断言内容 |
| --- | --- |
| A · 规则覆盖 | 白名单语义下漏配一行 = 该迁移**静默被禁止**（无报错），因此每个场景/动作必须覆盖全部可达来源状态 |
| B · 安全关键禁止 | 已完成任务不可回退、已退订订单不可再流转、配送中订单不可退订等；未配置组合同样默认禁止 |
| C · 台账可回放 | 用迁移台账重放一遍，必须得到与真实状态一致的结果（无缝隙、无跳跃） |
| D · 不变量执行顺序 | 每条声明依赖都排在本项之前（拓扑性）、关键序对成立、顺序确定可复现，**且不是字典序的巧合** |

**实测数据**（`target/experiment-reports/StateMachineContractTest.txt`、`InvariantDependencyOrderTest.txt`）

```text
【A】订单动作数 5 / 任务动作数 5
     应覆盖的（场景/动作/来源状态）组合数   35
     实际缺失组合数                    0
【C】台账条数                          3
     回放路径                          1 → 2 → 3 → 4
     回放得到的状态                      4（与实际状态一致）
【D】执行顺序     INV_ORDER_TASK → INV_ORDER_AGGREGATION → INV_PARENT_TERMINAL_CHILD_PENDING
                 → INV_PAYMENT_CONSISTENCY → INV_QUOTA_LEDGER → INV_SIGN_INTAKE → INV_TASK_RECORD
     字典序（对照） INV_ORDER_AGGREGATION → INV_ORDER_TASK → …（前两项相反）
     网格补全 位于父聚合之前                0 < 1
     父聚合 位于组合守卫之前                1 < 2
     两次解析结果一致（确定性）              true
     违反声明依赖的项（应无）                （无）
```

**结论**

- 规则覆盖矩阵是**白名单语义的必要配套**：漏配一行不会报错，只会让某个迁移在某天突然被拒——
  这个测试把它变成编译期内（执行期）可见的失败；
- 台账可回放矩阵把「过程可观测」从口号变成断言：`process_transition_log` 足以完整重放订单的
  每一次状态变化，且每一步首尾相接。

### 6.9 实验十三：配送日平移与拒收补送

**命题**：平移的"合并/新建"两条路径都只产生一次效果且可重试；拒收补送按订单类型分叉
（套餐合并 `quantity`、零散新建任务 + 追加次日机动配额）；补送落库先于父订单完成判定；
期末摊平保留补送量、作废窗口外待配送任务且总量守恒。

**设计**：

| 用例 | 对照 / 关键构造 |
| --- | --- |
| 平移 | 套餐 3 天各 1 盒：`day1→day2`（**次日已有同键任务**，走合并）→ 同参数重试 → `day3→day4`（**周期外**，走新建）→ 对配送中任务平移（预检应拒绝） |
| 零散拒收 | 单日订单拒收：次日**无**任务 → 新建 + 追加次日配额；断言订单仍为「配送中」 |
| 套餐拒收 | 多日订单拒收：次日**有**任务 → 合并 `quantity`；断言**不写**配额台账（套餐不占机动池） |
| 期末摊平 | 6 天订单，人为把 d2 造成"已有补送"（数量 2 + 一条补偿台账），再摊平到 d4；断言补送量未被抹掉、总量守恒、重复执行不调整 |

**实测数据**（`target/experiment-reports/Experiment13DeliveryShiftCompensationTest.txt`）：

```text
平移（合并 / 新建 / 幂等 / 预检）
  首次 shifted/merged/created/skipped     1 / 1 / 0 / 0
  day1 任务状态（期望 4 已取消）            4
  day2 数量（期望 2 合并）                2
  原记录签收状态（期望 2 未签收，未被置拒收）  2
  重试 shifted/skipped（期望 0 / 1）       0 / 1
  重试后 day2 数量（期望仍 2）             2
  新建 shifted/created（期望 1 / 1）       1 / 1
  day4 状态/数量（期望 1 / 1）             1 / 1
  配送中任务平移（期望拒绝）                BusinessException

零散订单拒收（新建 + 配额）
  补偿台账 1 / 原任务状态 4 / 次日任务数量 1（状态 1）
  次日配额已售 1 / 次日台账 1 / 拒收原因 SOUR / 订单状态 3

套餐订单拒收（合并，不占配额）
  补偿台账 1 / 次日任务数量 2 / 次日签收记录数量 2 / 配额台账行数 0 / 订单状态 3

期末摊平（保留补送 + 尾部作废 + 守恒 + 幂等）
  首次调整条数 2
  d2 数量（期望 3 = 摊平计划 2 + 补送 1）    3
  窗口内待配送总量（期望 7，与摊平前守恒）     7
  d5 / d6 状态（期望 4 作废）              4 / 4
  重复执行调整条数（期望 0）                0
```

**结论**：

1. **平移的两条路径都只生效一次**：重复执行时 CAS 挡住，`day2` 数量不变——CAS 本身就是幂等闸门，
   不需要额外的批次表；合并时原签收记录保持未签收，未被误置为拒收。
2. **拒收补送按订单类型分叉成立**：零散订单次日新建任务并追加次日机动配额，套餐订单合并 `quantity` 且不写配额台账；
   两者都让订单停留在「配送中」——这直接验证了"补送落库先于完成判定"的顺序约束（否则补送任务会被组合守卫自动作废）。
3. **期末摊平在"已有补送"时仍守恒**：补送量未被重置抹掉，窗口外任务被作废，且可重复执行（第二次调整 0 条）。

**本组实验暴露的缺陷**：见第 7 节 **D7** ——摊平把"待配送总量"（其值**已包含补送盒数**）当作可分配计划量，
分配时又在目标量上加一次补送，导致补送被重复计入：总量由 7 变 8、且重复执行不收敛。
**这正是补实验的价值：若不补这组用例，这个守恒错误不会被任何断言发现。**

### 6.10 实验十四：周末停送与调休例外（配送日历重排）

**命题**：把"周末不送 + 调休例外"从人工逐条平移升级为按日历自动重排后，仍需满足与前一轮平移完全相同的不变量：
① 盒数与任务数守恒；② 重复执行幂等；③ 历史（已配送/配送中）不可改；
④ 单日上限 3 盒不被突破；⑤ 开关关闭时行为与现状一致（默认不生效）。

**设计**：

| 用例 | 对照 / 关键构造 |
| --- | --- |
| 日历重排 | 套餐订单覆盖 `[今天, 周六+8]`：本周六 → 周四、本周日 → 周五（各 +1 盒）；**下一个周六标为补课日**（type=2，应不重排）；**周二标为停送日**（type=1，应并入前一个工作日周一）；随后**同参数重跑一次**验幂等 |
| 单日上限前推 | 先把"周六提前 2 天"的**首选目标日（周四）顶满 3 盒**，断言周六的奶不落在周四（保持 3）而前推到周三（2 盒） |
| 开关闸门 | `delivery.weekend.stop=false` 时执行，断言抛 `BusinessException`（不改动现状行为） |

**实测数据**（`target/experiment-reports/Experiment14DeliveryCalendarRebalanceTest.txt`）：

```text
日历重排（周末 / 补课 / 停送 / 守恒 / 幂等）
  首次 requested/shifted/merged/created/skipped   5 / 4 / 4 / 0 / 1
  周六任务状态（期望 4 已取消）                    4
  周四数量（期望 2 = 自身 1 + 周六并入 1）          2
  周日任务状态（期望 4 已取消）                    4
  周五数量（期望 2 = 自身 1 + 周日并入 1）          2
  补课日（周六）状态/数量（期望 1 / 1 不重排）       1 / 1
  停送日（周二）状态（期望 4 已取消）               4
  前一个工作日（周一）数量（期望 2）                2
  在途盒数（重排前 / 重排后，期望相等）             15 / 15
  重复执行 shifted（期望 0 幂等）                  0

单日上限前推
  周六任务状态 4 / 周四数量（未被突破）3 / 周三数量 2

开关闸门
  开关关闭时执行结果（期望 BusinessException）      BusinessException: 「周末停送」未开启…
```

**结论**：

1. **自动重排与原手工平移的不变量完全一致**：在途盒数 15 → 15 守恒，重复执行 `shifted=0`（CAS 作废后源任务
   不再是待配送，天然幂等），因此重排可以直接替代"逐条勾选平移"的学期初操作。
2. **例外表语义成立**：补课日（type=2）的任务留在原配送日不被动；停送日（type=1）并入前一个有效配送日——
   两处都是"管理员写了什么就是什么"，系统不做任何官方日历推算。
3. **上限是"前推"而不是"拒绝"**：首选目标日已满 3 盒时继续向前找未满的工作日（周四保持 3、周三变 2），
   长假集中（如连休把多天奶压向同一工作日）能自动向前摊开，减少了方案原文"拒绝并人工兜底"的运维负担。
4. **默认不生效**：开关关闭时直接拒绝执行，新功能对现状零影响——这一点由第三条用例显式守住。


### 6.11 实验十五：退款与毕业清算

**命题**：退款域作为"继支付域之后第二个长期、多阶段、可失败过程"，在复用既有四层模型与可靠性机制后，仍需满足：
① 并发申请只产生一张进行中退款单（幂等由数据库唯一键仲裁）；② 重复执行退款只落账一次（任务只作废一次、台账只记一次）；
③ 部分配送后退款金额守恒且**尾差按累计制自找平**；④ 退款与配送并发时"作废成功 ⇔ 钱按它结算"，不存在"退了钱又送奶"；
⑤ 缺货取消（没送奶）的期次可退，且配额不重复回补；⑥ 毕业清算幂等、单笔失败不阻塞其余。

**设计**：

| 用例 | 对照 / 关键构造 |
| --- | --- |
| 并发申请幂等 | 10 天套餐订单，**8 线程**同时申请 → 断言只有 1 张进行中单落库（`uk_refund_active` 生成列唯一键仲裁） |
| 并发执行退款 | 5 天套餐（实付 15.00），**6 线程**同时执行同一退款单 → 断言仅 1 次生效、5 条任务只作废一次、`EXECUTE` 台账 1 条；随后驱动实时通道，断言订单收敛为已完成(4) |
| 部分配送后金额守恒（尾差） | 3 天套餐把实付改成 **10.00**（10/3 除不尽）；第 1 天签收（不可退）后对剩余 2 天退款 → 断言预览 2 盒/6.67 与实际一致，且 `6.67 + 3.33（已配送 1 盒的四舍五入值）= 10.00` |
| 退款与配送并发竞争 | 2 天 × 2 盒（实付 12.00），退款执行与"开始配送"**零栅栏并发** → 断言 `作废盒数 + 已送出盒数 = 4`、金额 = `12.00 × 作废盒数 / 4`、每条任务只落在"已送出"或"已作废"之一 |
| 缺货取消期次可退 | 单日零散单走真实支付链路（扣 2 盒配额）→ 缺货取消（配额回补为 0、订单被聚合为已完成）→ 对该**已完成订单**退款 → 断言 2 盒可退、金额 6.00（明细单价精确计价）、配额仍为 0（不重复回补也不减成负数） |
| 毕业清算幂等 | 同一学生名下 1 张待支付单 + 1 张已支付单（3 天 × 1 盒）→ 清算两轮 → 断言第一轮 1 取消 + 1 退款/9.00 元、第二轮 `totalOrders = 0` |
| 申请幂等与重新申请 | 进行中单阻塞第二次申请；审核**拒绝(4)** 后重新申请得到新单（历史单不阻塞） |

**实测数据**（`target/experiment-reports/Experiment15RefundSettlementTest.txt`）：

```text
并发申请幂等
  并发线程数 8 / 申请成功 1 / 申请失败 7 / 该订单退款单总数 1 / 其中进行中 1
  失败原因：7 × BusinessException: 该订单已有进行中的退款单，请勿重复申请

并发执行退款
  并发线程数 6 / 执行成功 1 / 执行失败 5
  已退款单数 1 / 退款单状态 3 / 退款盒数 5 / 退款金额 15.00
  已作废任务数 5 / 仍待配送任务数 0 / 退款执行台账条数 1
退款后父订单收敛（实时通道）
  订单状态（期望 4 已完成） 4

部分配送后退款金额守恒（尾差）
  预览可退盒数 / 预估金额 2 / 6.67
  实际退款盒数 / 金额 2 / 6.67
  已签收期次价值（10/3 四舍五入） 3.33
  已退款 + 已签收价值 10.00
  已签收任务状态（不受退款影响） 3

退款与配送并发竞争
  成功数 / 失败数 2 / 0
  退款盒数（实际作废成功） 2 / 退款金额 6.00（= 12.00 × 2 / 4）
  已送出盒数 2 / 作废盒数 + 已送出盒数 4 / 任务归属：已送出 1 / 已作废 1

缺货取消期次退款
  支付后占用配额 2 / 缺货取消后 0（已回补）/ 退款后 0
  缺货取消后订单状态 4（全部任务终态已聚合）/ 退款盒数 2 / 退款金额 6.00

毕业清算（混合订单 + 幂等）
  第一轮待清算 2（取消待支付 1 / 退款 1 / 跳过 0 / 失败 0）/ 退款合计 9.00
  待支付单状态 5 / 已支付单状态 4 / 已退款单数 1 / 第二轮待清算 0

申请幂等与重新申请
  重复申请被拒：BusinessException: 该订单已有进行中的退款单，请勿重复申请
  首单状态 4（已拒绝）/ 重新申请得到新单（≠ 首单）
```

**结论**：

1. **幂等与一致性由数据库仲裁，不靠应用层自觉**：并发申请 8 → 1（生成列唯一键），并发执行 6 → 1
   （退款单 CAS + 任务 CAS + 同一事务），三处"只一次"同时成立（落账一次、作废一次、台账一次）。
2. **"先作废、后计价"是金额正确的前提**：竞态用例里失败方没有被计价——作废盒数与已送出盒数严格互补，
   金额恰好等于 `实付 × 作废盒数 / 合同总盒数`。若改成"先按候选集计价"，这条断言必然失败。
3. **累计制把尾差消化在公式里**：10.00 元 3 盒退 2 盒得 6.67，与已配送 1 盒的 3.33 相加恰为 10.00；
   逐笔独立四舍五入会漂移到 6.66/6.67 这类组合。
4. **R1 判据的两个边界都被实测覆盖**：缺货取消（没送奶）可退——即使订单已被聚合为**已完成(4)**；
   而平移/退款自身作废的任务因不置签收记录为拒收，不会被二次退款。
5. **退款不侵入订单状态机**：退款侧只入队父聚合待办，订单由实时通道按规则表收敛为已完成，
   `state_transition_rule` 的订单场景**零新增**（实测收敛到 4）。
6. **清算与"超时取消"必须分道**：待支付单走专属路径后配额/流水/状态干净（取消 1 单、退款 1 单、失败 0），
   第二轮 `totalOrders = 0` 证明可重复执行安全。

### 6.12 退款域 HTTP 契约测试（接口层，非并发实验）

**命题**：实验十五证明退款**逻辑**正确，但前端是按 `接口文档.md` §10 的路径与报文调用的——
一旦有人改了 URL、HTTP 方法或参数名，前后端会"各自都对、合起来不通"。因此需要一层**接口层断言**把
「路径 / 报文 / 角色闸门」钉死；退款涉资金，三条安全底线（未登录不可用、班主任与配送站不接触退款、
家长只能操作本人绑定学生）必须在接口层被验证，而不是"前端不显示按钮"。

**设计**：`@SpringBootTest` + `@AutoConfigureMockMvc` + 实验库；
管理员/教师账号用种子用户（`admin`/`teacher`/`delivery`，密码 `123456`）登录取 JWT；
家长 token 由 `JwtTokenProvider` 直接签发（`/api/auth/login` 按设计**拒绝纯家长账号密码登录**，家长走小程序微信登录）。
5 个用例：未登录闸门、非管理角色闸门、管理员全链路、家长数据范围与写操作隔离、毕业清算幂等。

**实测数据**（`target/experiment-reports/RefundApiContractTest.txt`）：

```text
未登录闸门
  GET /api/refund/list 401 / PUT /api/refund/{id}/execute 401 / POST /api/student/{id}/settle 401
角色闸门（班主任 / 配送站）
  TEACHER GET /api/refund/list 403 / TEACHER PUT /api/refund/{id}/execute 403
  TEACHER POST /api/student/{id}/settle 403 / DELIVERY GET /api/refund/list 403
管理员全链路
  预览可退盒数 / 预估金额 3 / 9.00 → 申请 → 审核通过 → 执行退款
  执行后：已退款单数 1（重复执行不新增）/ 已作废任务数 3 / 重复执行返回业务码非 200
家长数据范围与写操作隔离
  本人订单预览 200 / 本人订单申请成功 / 他人订单预览业务码 403 / 他人订单申请业务码 403
  家长审核 HTTP 403 / 家长清算 HTTP 403
毕业清算接口
  第一轮待清算 2（取消待支付 1 / 已退款 1 / 跳过 0 / 失败 0）/ 退款合计 9.0
  第二轮待清算 0（幂等）/ 已支付单收敛为已完成(4)
```

**结论**：

1. **前后端契约有守卫**：路径、方法、请求体字段（`applyBoxCount`/`reason`、`approved`/`remark`）与
   响应字段（`data.refundableBoxes`/`estimatedAmount`/`list[].status`）在接口层被断言，前端改调用或后端改契约会立刻红。
2. **两类 403 的分工被显式记录**：SecurityConfig 层的角色拒绝是 **HTTP 403**；Service 层数据范围校验
   （家长越权）抛 `BusinessException(403)` → **HTTP 200 + 业务码 403**，由前端 `utils/request` 统一提示。
   这条区分写进用例，避免后来者误把"HTTP 200"当成越权放行。
3. **退款单侧闸门在接口层同样成立**：重复执行返回非 200 业务码（`REFUND/EXECUTE/3 = 0`），
   已退款单数保持 1、任务不重复处理——与实验十五的服务层结论互相印证。

### 6.13 实验十六：批次追溯钩子（召回反查）

**命题**（对应 `论文/后续扩展方案.md` §4.7）：批次只是**配额池上的可选标注**——不参与扣减、结转与任何状态流转，
但必须能回答"这批奶送给了哪些孩子"（批次召回、监管检查）。本实验只验三件事：反查链路是否完整、
两个边界是否成立、以及它与结转语义是否冲突。

**设计**（`Experiment16BatchTraceTest`，3 用例）：

| 用例 | 构造 | 断言要点 |
| --- | --- | --- |
| 链路完整 | 建批次档案 → 配额池标注批号 → 零散下单支付（扣 2 盒）→ 送出 → 签收 → 反查 | 关联池子 1、台账合计 2 盒、清单 1 行；订单/学生/班级/盒数/配送日齐全；任务状态 3、签收状态 1 |
| 两个边界 | ① 批号**不建档**直接标注并下单；② 查一个从未标注的批号 | ① 档案为空但**照旧反查**（池子 1 / 明细 1 行）；② 池子 0 / 明细 0 / 盒数 0，**不抛异常** |
| 与结转共存 | 只给**配送前一天**建池并标注（配送当日不建池）→ 下单支付 | 台账落在前一天（先消耗最老的池子）：`quotaDate=9/22`、`deliveryDate=9/23` 分列；任务按配送日命中 |

**实测数据**（`target/experiment-reports/Experiment16BatchTraceTest.txt`）：

```text
批号反查链路
  批次档案 EXP587919-B1 / 关联池子 1 / 命中台账盒数 2 / 清单 1 行
  订单 / 学生 / 班级  EXP587919-O3 / EXP587919学生 / EXP587919班
  池子日期 / 配送日  2026-09-22 / 2026-09-22
  任务状态 / 签收状态  3（已完成）/ 1（已签收）
未建档与未标注边界
  未建档批号可反查  档案为空、池子 1；明细 1 行
  未标注批号        池子 0、明细 0
结转与批次共存
  配送日 / 扣减池子日  2026-09-23 / 2026-09-22；命中 1 盒；配送任务存在
```

**结论**：

1. **钩子的价值在"可回答"而不在"参与流转"**：批次全程没有进入任何状态机、扣减或台账口径，
   却把"批号 → 池子 → 台账 → 学生 → 任务/签收"这条召回链完整打通——这正是"为业务决策建模、
   不为物理现实建模"的落地形态。
2. **两个边界是刻意的**：事故当场**不应先建档才能查**（未建档可反查），
   查询**不应因"没命中"而报错**（未标注返回 0/空）——否则召回现场会被系统挡住。
3. **与结转共存的结论要写清**：池子日期与配送日**必须分列**。结转把"9/22 的池子"给了"9/23 的订单"，
   若把两者合成一个"日期"字段，就会出现"批次 A 送给了 9/23 的孩子"这种**看起来精确、实际错位**的结论。

> 未覆盖：批次反查**只覆盖单日零散订购**（学期套餐不经配额池，无批号标注）；
> 套餐用奶的召回需按配送日与奶站人工对照。这条边界已写入 `研发规范/项目开发规范.md` §5.2 与接口文档。

### 6.14 实验十七：奶站「当日未送达申报」

**命题**：自动签收兜底的候选集判定用的是**信息态**（配送站点了「今日已送出」），而奶站申报表达的是
**物理态**（奶实际没到校）。两者混同会让兜底把"没送到"签成"已签收"，并**在同一事务内生成营养摄入**
——一条虚假签收 + 一份虚假摄入，都是"已提交业务事实"。本实验验证三件事：申报是否真的把任务挡在兜底之外、
守卫与幂等是否成立、以及"跟进"是否真的不动状态。

**设计**（`Experiment17UndeliveredReportTest`，3 用例）：

| 用例 | 构造 | 断言要点 |
| --- | --- | --- |
| 排除效果 | 昨日零散单支付 → 批量送出 → **申报未送达** | 申报前在候选集 / 申报后不在；`autoSignOne` 单条入口也跳过：签收状态仍 2、任务仍 2、营养摄入 0 条、SIGN 台账 0 条 |
| 幂等与前置 | 重复申报；任务签收（已完成）后再申报 | 重复申报抛业务异常且库中仍 1 行；非「配送中」任务被拒绝 |
| 跟进只登记 | 申报 → 标记已跟进 → 重复点击 → 从待办列表回读 | 待跟进 1 → 已跟进 1；跟进说明不被重复点击覆盖；**任务状态仍为 2**；列表回显任务号/学生/班级/盒数 |

**实测数据**（`target/experiment-reports/Experiment17UndeliveredReportTest.txt`）：

```text
申报后排除出自动签收
  申报前在候选集 true / 申报后不在候选集 false
  单条自动签收后：签收状态 2（未签收）/ 任务状态 2（配送中）/ 营养摄入 0 条 / SIGN 台账 0 条
幂等与前置守卫
  重复申报 → BusinessException「该任务已申报过未送达，无需重复申报」；申报表行数 1
  已签收后再申报 → BusinessException「仅「配送中」（已送出、未签收）的任务可申报未送达」
跟进只做登记
  待跟进 1 → 已跟进 1；跟进说明「已线下补送，家长已确认」（重复点击未覆盖）；跟进人 admin
  任务状态仍为 2（配送中，本操作不改状态）
  回显 任务号 DT20260920-200-308 / 学生 EXP310470学生 / 班级 EXP310470班 / 1 盒
```

**结论**：

1. **"物理/信息混同"是可被定位、可被修复的一类缺陷**：不引入新状态、不写任何状态迁移，
   只加一条**业务侧可声明的例外通道**，就把"兜底替人签收"这个错误从"会发生"变成"不会发生"。
2. **两道闸门缺一不可**：候选集过滤 + 单条入口再判。候选集是分批取的（每批 ≤200），
   两次取之间奶站仍可能补报；只在候选集过滤会漏——**"一定签不掉"的保证来自单条入口这一判**。
3. **申报与处置分离**：申报只打标记与待办，处置仍走签收/拒收/取消出口（因此照旧经统一迁移出口与台账）。
   这与"人工介入通道"（`后续扩展方案.md` §4.3）是同一条设计脉络，但当前刻意只做到"可见 + 不误签"。

> 未覆盖：真实的多实例并发申报/处置竞争（本用例为单线程构造）；
> 申报后的"长期无人处置"没有自动升级机制（刻意的边界，见 `可靠性设计.md` §十六）。

### 6.15 实验十八：家长端「当日豁免」

**命题**：家长当天临时不要这份奶（病假/外出）时，应能取消**今天尚未送出**的任务；同时三件事不得被绕过：
取消仍走统一出口（留痕）、次数上限严格不超限、只碰"今天 + 待配送"且零散订购配额按台账回补。

**设计**（`Experiment18ParentExemptionTest`，3 用例；家长身份用 sys_user + `sys_user_role`(PARENT) 构造，
与小程序微信登录后的 JWT 语义一致）：

| 用例 | 构造 | 断言要点 |
| --- | --- | --- |
| 正常豁免 | 今日零散单支付（2 盒）→ 家长豁免 | 任务 4（已取消）、签收记录 3（联动拒收）、CANCEL 台账恰 1 条；配额 2 → 0；计数 1/3、剩余 2 |
| 限次闸门 | 计数器预置为已用 3/3 → 再豁免 | 抛业务异常、任务仍待配送(1)、**配额未被回补**（动作整体回滚） |
| 前置边界 | ① 先批量送出再豁免；② 换成未绑定该学生的家长 | ① 被拒（且任务仍为配送中 2）；② 被数据范围拦住（"没有可豁免的待配送任务"） |

**实测数据**（`target/experiment-reports/Experiment18ParentExemptionTest.txt`）：

```text
正常豁免
  豁免前可豁免任务数 1 / 本月已用 0（上限 3）
  豁免后：任务状态 4（已取消）/ 签收记录 3（拒收）/ CANCEL 迁移台账 1 条
  配额：支付后已用 2 → 豁免后已用 0（按台账回补原池）
  计数：本月已用 1 / 剩余 2
限次闸门
  概览 已用/上限/剩余 = 3/3/0；第 4 次 → BusinessException「本月「当日豁免」次数已用完（3/3 次）」
  任务状态 1（待配送，未被取消）/ 配额已用 1（未回补）
前置边界
  已送出后豁免 → BusinessException「今天没有可豁免的待配送任务」；任务状态 2（配送中）
  他人学生豁免 → 同样被数据范围拦住
```

**结论**：

1. **"次数上限"是资源，不是计数**：本项用 `行锁读 + used_count + 1 <= limit` 条件更新，
   与配额池同一套并发语义；`COUNT(*) < N` 的写法在并发下会超限（实验十八只验证了串行路径，
   并发路径的正确性来自写法本身与实验一/十二已验证的同一模式）。
2. **复用出口带来"免费"的确定性**：豁免走 `doCancelTask`，所以"CANCEL 台账恰 1 条"、
   "签收记录联动置拒收"、"父过程自愈待办"全部自动成立——不需要为家长入口再写一遍不变量断言。
3. **同事务 ⇒ 失败即回退**：限次用例里配额未被回补，正是因为动作被拒后整体回滚；
   这比"先扣次数、再尝试取消"更安全（后者会留下"扣了没生效"的账面）。

> 未覆盖：真实多实例并发豁免（同一学生两个请求同时打进来）未构造；撤回豁免刻意不做（见
> `可靠性设计.md` §十七.5）。

### 6.16 实验十九：过敏/禁忌软警示（受控枚举 + 交集命中）

**命题**（"警示优于拦截"）：命中判定 = 学生禁忌 ∩ 奶品过敏原。这条判定最容易的死法不是"写错逻辑"，
而是**两侧标签永远匹配不上**（学生填"乳糖不耐"、奶品填"乳糖"）——警示静默失效，比没有警示更危险。
因此本实验同时验证"命中判定"与"写入规范化"。

**设计**（`Experiment19AllergyWarningTest`，3 用例）：

| 用例 | 构造 | 断言要点 |
| --- | --- | --- |
| 交集命中 | 学生 `LACTOSE,NUTS` × 奶品 `LACTOSE,SOY` | 命中 1 条、编码 `[LACTOSE]`、中文 `[乳糖]`、提示语含奶品名与"乳糖"；受控选项 7 项且含两侧文案 |
| 不命中 | ①交集为空 ②学生无标签 ③奶品无标签 | 三种情况均返回空清单（0 误报） |
| 写入规范化 | 脏输入 / 未知编码 / 清空，均经 `ProductService.updateProduct` 走真实写入路径 | 脏输入落库为 `LACTOSE,NUTS`；未知编码抛业务异常；**空串清空真正落库**（对抗 MP 跳过 null 的坑） |

**实测数据**（`target/experiment-reports/Experiment19AllergyWarningTest.txt`）：

```text
交集命中
  命中 1 条：EXP846604奶 / [LACTOSE] / [乳糖]
  提示语：「EXP846604奶」含 乳糖，与「EXP846604学生」的禁忌标签一致，请确认是否仍要下单
  受控选项 7 项；LACTOSE 文案 奶品侧「乳糖」/ 学生侧「乳糖不耐」
写入规范化
  脏输入 " lactose , NUTS ,lactose, " → 落库 LACTOSE,NUTS
  未知编码 → BusinessException「未知的过敏原编码「NOT_A_CODE」，可选：LACTOSE/NUTS/…」
  清空（空串）→ 落库 空串；parse("")=0；normalizeStrict(null)=""；parse("LACTOSE,BOGUS") 只保留 LACTOSE
不命中
  交集为空 / 学生无禁忌 / 奶品无过敏原 → 均 0 条
```

**结论**：

1. **"受控枚举"是这条功能能生效的前提**：若不强制规范化，两侧自由文本会让命中率长期为 0，
   而界面上一切正常（标签都存进去了）。**这类"配置看起来生效、实际永不命中"的失效模式值得单独记录。**
2. **空串清空是必要的接口约定**：MyBatis-Plus 默认跳过 null 字段，"清空标签"若用 null 表达会被静默忽略
   （用户看到"保存成功"但标签还在）。实测空串路径落库为空串、可再次正确读取。
3. **只提示不拦截的取舍**：本实验不验证"下单被拦"（因为刻意不拦），验证的是"命中能被识别、能被正确告知"；
   拦截与否的决策权交还用户（禁忌档案可能滞后、家长可能明知而仍要买）。

> 未覆盖：命中后的用户确认流程属前端交互（小程序/管理端），未做自动化测试；
> 标签的 Excel 批量导入不支持（导入模板未含该列），需在界面上逐条维护。

### 6.17 实验二十：仓库余量台账与配额发行封顶

**命题**：履约链起点（奶企 → 学校配送站 → 领取点 → 班级）原先不在系统里，
"多出来的奶没有归宿、短交发现太晚、配额发行没有物理依据"。把**仓库余量台账**引入后，
必须满足：① 余量恒等式在任何混合负载后成立；② 出库与退回**与业务动作同事务**且幂等；
③ 配额发行被仓库实物封顶；④ 短交在收货时就暴露但不阻断作业；
⑤ 台账判据必须由**事实字段**决定而不是由多来源的终态推断。

**用例与实测数据**（`target/experiment-reports/Experiment20WarehouseLedgerTest.txt`，7 用例）：

```text
① 守恒（混合负载：到货 / 送出 / 拒收退回 / 修正）
  期初 INIT                                       1,000,000
  到货 IN / 退回 IN_BACK / 修正 ADJ / 送出 OUT     104 / 2 / 2 / 2
  服务算出的 W                                     1,000,106
  恒等式重算的 W（独立 SQL 重算）                   1,000,106

② 出库幂等（16 线程并发批量送出，4 个任务）
  累计 dispatched 返回值（期望 4）                  4
  OUT 台账行数（期望 4）                            4
  单个任务最大 OUT 行数（期望 1）                    1
  并发失败摘要                                     （无失败）

③ 发行封顶
  W=50 时设配额 100                                BusinessException：仓库余量不足…该日实物需求 100 盒 > 仓库余量 50 盒
  拒绝后配额行数（期望 0，未落库）                   0
  登记到货 60 后的 W / 补货后设配额 100              110 / 通过（落库 100）

④ 短交预警
  应到（池剩余 + 待送出任务）/ 实到 / 缺口           100 / 80 / 20
  IN 台账行数（预警不阻断登记）                     1

⑤ 退回闭环（送出 → 拒收 → 次日补送）
  送出后 W −1 → 拒收回仓后 0 → 次日补送出库后 −1
  原任务 OUT 1 条 / 原记录 IN_BACK 1 条 / 补送任务 OUT 1 条（单条路径钩子）

⑥ 多实例并发登记（8 线程，同品种同日同凭证号）
  成功 1 / 被唯一键挡回 7 / IN 台账行数 1 / 余量增量 10（只落账一次）

⑦ 不变量 INV_LEDGER_OUT_TASK
  注入：删除已送出任务的 OUT 行                     1
  负向对照：1→4 未送出任务（不得计入）               1
  检出数 / 自动修复数（期望 1 / 1）                  1 / 1
  补写后 OUT 行数 / 补写的 biz_date                 1 / 2026-09-21（= dispatch_time 当日）
```

**结论**：

1. **恒等式是"账实对齐"的最小闭环**：`W = ΣIN + ΣIN_BACK + ΣINIT + ΣADJ(带符号) − ΣOUT` 在
   到货/送出/退回/修正混合负载后由**两条独立路径**（服务实现 / 测试内独立 SQL）算出同一结果，
   说明它不是"用被测代码验证被测代码"。
2. **出库幂等靠"CAS 抢占 + 业务唯一键"两层**，与既有实验三/二同款：16 线程反复批量送出，
   4 个任务恰好 4 条 OUT、每任务最多 1 条，**且无失败**——因为落败方在 CAS 处即中止，不会去写台账。
3. **封顶是"显式失败优于静默"的又一实例**：W=50 时设 100 被拒且**未落库**（事务回滚），
   补货 60 后放行；失败消息把公式的三个分项都打印出来，管理员当场知道该补货还是该下调配额。
4. **短交预警不阻断**：应到 100 实到 80 → 预警消息 + 登记成功 + 余量按实到 80 入账。
   这与"企业分批到货是常态"的业务事实一致；真正的闸门是发行时的封顶。
5. **退回闭环首尾相接**：送出 −1 → 拒收回仓 0 → 次日补送再 −1；
   并且**单条送出路径**（`startDelivery`）与批量路径都记了 OUT——这正是设计评审 P0-4 指出的漏账点，
   本用例是它的回归守卫。
6. **⑦ 是本次改动最重要的守卫**：删除已送出任务的 OUT 行会被自动补写（`biz_date = dispatch_time` 当日），
   而同一次体检里那个"1→4 从未送出"的任务**零误判**。若判据写成"任务状态 ∈ {2,3,4}"，
   这条用例会以"补写出假出库行"的方式失败（见第 7 节 D8）。

> 未覆盖：真实多主机/多进程下的并发出库（实验九的单 JVM 多实例模拟已覆盖共享数据库语义）；
> 前端页面（配送站登记、管理端台账）未做自动化测试；
> 出库**不做余额校验**（送出是物流事实，缺货只体现为 W 变小与不变量告警），因此没有"余量不足拒绝送出"的用例。

## 7. 缺陷台账与回归验证

这是本轮实验最有价值的部分：**实验的目的不是证明设计有效，而是把设计里的洞挖出来**。

| 编号 | 现象 | 根因 | 修复 | 回归守卫 |
| --- | --- | --- | --- | --- |
| D1 | 并发展开任务会重复生成（理论 6 条可能变 12 条） | 幂等靠应用层「先 `selectCount` 再 `insert`」，并发下双双查不到再双双插入 | 配送任务加业务唯一键 `uk_order_product_date`；`IdempotencyGuard` 把唯一键冲突翻译为「已存在」并幂等跳过；配额台账同理加 `uk_order_product_pool` | 实验三（任务数恒等于理论数） |
| D2 | 并发回调被「状态机规则禁止」误拒（19/20） | `StateMachineServiceImpl.refresh()` 用 `cache.clear()` + `putAll()` 刷新；并发读取方在「已清空、未填完」窗口内读到空规则表，白名单语义下等于**瞬时全禁** | 改为「先构造完整新表，再一次性替换 volatile 引用」；并加 `@PostConstruct` 启动预热，消除首批请求落在空缓存上的可能 | 实验二新增断言：失败必须是 CAS 冲突抛异常，不能是规则表静默拒绝 |
| D3 | 并发扣减报 `Record has changed since last read in table 'daily_quota'`（有保护版本只成功 25/200） | 同一事务内先对某行做**一致性快照读**、再对该行做**加锁读/更新**。MariaDB 在 REPEATABLE READ 下会直接拒绝，MySQL 则静默改读最新版本——同一份代码在两种数据库上行为不一致 | ① 扣减改为一次性 `SELECT ... FOR UPDATE` 行锁读，不再混用「快照读校验 + 加锁写回」；② 统一数据源隔离级别为 `READ_COMMITTED`（本系统并发安全全部由条件更新/行锁/唯一键显式保证，不依赖可重复读语义） | 实验一对照 B（成功数恰好等于配额、零超卖） |
| D4 | 修复 D2 后，19 次并发回调改为撞业务唯一键 `uk_order_product_pool` | 副作用（扣配额、写支付流水）发生在状态抢占（CAS）**之前**：落败方已经做了副作用才失败，只能靠回滚收场，并发下互相撞唯一键 | 调整为**先抢占状态、再执行副作用**：`payOrder` / `handleWechatPayNotify` 都先做 CAS，落败方立即中止，配额扣减/流水写入/任务展开只由胜出者执行一次 | 实验二（失败原因全部为「订单状态已变更，回调处理冲突」） |
| D5 | 「每一次状态迁移都写入台账」存在空洞：退订/缺货取消联动作废签收记录时无台账记录 | `markRecordRejected` 绕过统一出口，直接 `deliveryRecordMapper.update` 改 `sign_status`，未经过执行器 | 改为按任务查询未签收记录后**逐条经执行器 `attempt` 提交**（宽松迁移 + 留痕） | 实验五（订单/任务/签收记录每一步迁移的台账条数均为 1） |
| D6 | 订单头与明细**并非原子写入**：`doCreateOrder` 上的 `@Transactional` 实际不生效 | `doCreateOrder` 是 public 却被同类 `createOrder` 内部直调，Spring AOP 代理被绕过，注解形同虚设——明细插入失败会留下「订单存在但明细不完整」的半截数据 | 事务边界上移到外部入口 `createOrder`，`doCreateOrder` 降为 `private` | ⚠️ 暂无自动化守卫（由清理审计发现，建议后续补 JUnit 用例） |
| D7 | 期末摊平后总量不守恒（7 盒变 8 盒），且重复执行不幂等 | 摊平把「待配送总量」当作可分配计划量，而该总量**已包含补送盒数**；随后又在目标量上加一次补送 → 补送被**重复计入** | 修正为「计划量 = 待配送总量 − 补送量」，摊平只重排计划量，补送量原样保留（`min(计划 + 补送, 3)`） | 实验十三（d2 补送日数量 3 = 计划 2 + 补送 1；窗口总量恒为 7；重复执行调整 0 条） |
| D8 | 仓库台账方案的两处"会算错账"的缺陷（**在设计评审阶段发现，未进入代码**）：① 配额发行封顶公式漏掉"已售未送出"的零散任务、又重复计入已送出的任务 → **可超发**；② 出库不变量用"任务状态 ∈ {2,3,4}"判"应已出库" → 1→4 的取消任务（缺货取消/家长豁免/平移作废/期末摊平）会被成批误判，而处置是 AUTO_REPAIR，**体检会自己补写出大量假出库行并把余量打成负数** | ① 任务在**支付事务内**就展开（扣配额与展开同事务），而 OUT 在**送出**时才记 → "已售未送出"既不在 `Σ(total−used)` 也不在原公式里；② `4-已取消` 是**多来源终态**（`1→4` 与 `2→4` 同码），状态本身不携带"是否送出"的信息 | ① 统一为「D 日实物需求 ≤ W」：`Σ_{池∈[D−2,D]}(total−used) + Σ_{status=1 且 delivery_date ≤ D}`，并对同品种发行加 `product` 行锁消除校验-动作竞态；② 判据改为 `dispatch_time IS NOT NULL`（与 CAS 同事务写入的唯一无歧义证据） | 实验二十 ③（W=50 设 100 被拒、补货后放行）与 ⑦（漏记自动补写 + 1→4 任务零误判）；`INV_WAREHOUSE_COVERAGE` 把同一关系式升级为持续验证 |

D1–D4 由实验一~四暴露；D5 是在"统一论文与代码表述"时逐条核对不变量发现的——**不变量只在被验证时才成立**；
D7 是实验十三在"给新功能补实验"时暴露的——**新功能不补实验，就不会有断言去发现它的守恒错误**；
D8 是仓库方案**在设计评审阶段**、逐条把口径对着代码核对时发现的（"把设计说明书里的公式代进真实的数据模型"）——
它说明验证层不只有"跑实验"一种形态：**在动手之前用代码事实检验设计公式，成本最低**。
D8 的两条都在实现时先落成实验二十的断言（③⑦），再写实现代码。

**共同规律**：D1/D2/D4 都是「检查与动作之间存在时间窗」导致的竞态——检查时条件成立、动作时条件已变。
三者分别用 **数据库唯一键**、**原子替换不可变快照**、**先抢占状态再动作** 三种手段消除窗口。
D3 是数据库语义差异问题，用 **显式行锁 + 统一隔离级别** 消除。
D5 是另一类问题：**不变量被"绕过统一出口"的旁路悄悄破坏**——它不会报错、不会丢数据，
只会让「可回溯」这个承诺悄悄失效。

### 7.1 补充用例（实验五）：状态迁移留痕完整性

「每一次状态迁移都写入台账」是过程可观测性的前提，但极易被破坏：只要有一条路径绕过统一出口
直接改状态，台账就会出现空洞。本用例走一条跨三张表、跨两级状态的完整链路（支付 → 退订联动），
逐项核对「最终状态 / 台账条数 / 资源回补」是否三方一致。

实测数据（`target/experiment-reports/Experiment5TransitionLedgerTest.txt`）：

```text
订单最终状态（期望 5 已退订）                   5
任务最终状态（期望 4 已取消）                   4
签收记录最终状态（期望 3 拒收）                 3
台账 ORDER/PAY 生效条数（期望 1）               1
台账 ORDER/CANCEL 生效条数（期望 1）            1
台账 DELIVERY_TASK/TASK_CANCEL 条数（期望 1）   1
台账 DELIVERY_RECORD/REJECT 条数（期望 1）      1
配额剩余（期望回补到 0）                        0
有效扣减台账行数（期望 0）                      0
已逻辑删除的台账行数（保留审计痕迹）             1
```

**判定**：三张表的每一次状态变更对应且仅对应一条「已生效」台账记录；配额按台账精确回补
（`used_quota` 归零、有效台账行归零）。

**附带发现（台账行的删除语义）**：`restore` 的「回补后删除台账行」实际是**逻辑删除**
（`DailyQuotaUsage` 带 `@TableLogic`），因此业务查询不可见、重复回补自然跳过，
但物理行仍存在并占用业务唯一键 `uk_order_product_pool`。当前业务路径不会触发问题
（同一订单同一池子不会二次扣减），但若将来出现该场景，必须先物理清理或调整键策略。
这一约束已记入 `../基线文档/可靠性设计.md`。

## 8. 结论

1. **资源一致性**：并发扣减在「行锁读 + 条件更新 + 台账唯一键」下零超卖，且资源被完整消耗；
   去掉保护后同样的负载会超卖 100 盒，证明该机制不是装饰。
2. **过程幂等**：同一动作重复执行（重复回调、重复展开）只产生一次有效副作用，
   且由数据库约束而非应用层判断保证，因而在并发下依然成立。
3. **异常恢复可配置**：父状态漂移的补偿规则已下沉到规则表，可在线启停；
   停用即不补偿、闸门禁止即不落地（实验六），说明「能力可配」没有换来「约束可绕」。
4. **恢复是持续发生的，而不是一次的**：7 类跨表不变量被周期求值，可逆项自动修复并复检闭环，
   涉资金项、终态项与在途任务只告警（实验七）；而且它连"机制组合"这一层都盖住了——
   实验七发现的组合异常先被组合守卫收敛，再通过**声明依赖 + 拓扑序 + 根因收窄**做到根本不产生；
   实时通道把不一致的不可检测窗口从 10 分钟压到约 5 秒，
   并在自身失败时退避、放弃、由兜底通道兜住（实验八）。
5. **过程可观测**：每次状态迁移都落入 `process_transition_log`，成功与冲突可区分；
   而且台账足以**完整回放**订单的每一次状态变化（契约矩阵 C）。
6. **多实例不再依赖任何实例本地状态**：支付状态落库后，跨实例确认扣款/查单/补偿全部打通；
   定时任务不选主也正确（写路径全是 CAS/唯一键/幂等跳过，"重复执行只浪费不犯错"）；
   编号与实例解耦（任务号由业务键确定、订单号带实例标识）——其中还顺手消除了一个隐藏缺陷：
   原实现会把"任务号撞了"误判成"业务键已存在"而静默跳过，**导致一条配送任务凭空消失**（实验九）。
7. **事务中间失败不留半截数据**：在支付事务的中间/末尾与签收事务的末尾注入异常，
   状态、流水、任务、配额、台账全部回到原点；被打断的 4 笔支付全部由对账补回，
   且补回后仍然只有一份副作用（实验十）。这说明"原子性靠事务，收敛靠通道"两条腿都要有。
8. **持续负载下三通道稳定收敛且延迟有界**：624 次人为漂移全部收掉（兜底 543 / 体检 71 / 实时 10），
   检测延迟 p95 216ms，最终零违规、零重复副作用（实验十一）。数据同时说明了双通道的必要性：
   **事件驱动的实时通道对"没有事件发生的写入丢失"无能为力**，而周期扫描的窗口又太长。
9. **"一致性优先"的代价被量出来了**：同一池子的并发扣减被行锁串行化——32 线程吞吐甚至低于
   8 线程（0.89×），p50 却涨到单线程的 9.8 倍；分散到多行后吞吐回升 4 倍（实验十二）。
   而且压力下正确性没有松动：约 10600 次并发扣减零失败、零超卖、账实一致。
   **性能实验同样断言不变量，这是本项目"性能不能让一致性妥协"主张的证据。**
6. **实验的副产品**：共 6 个真实缺陷被暴露并修复——D1~D4 由并发实验暴露（其中 D2、D4 只在并发下
   才可能出现，靠代码评审很难发现），D5 由「逐条核对不变量」发现，D6 由「清理死代码时的审计」发现
   （自调用导致 `@Transactional` 失效，订单头与明细非原子写入）。这本身就是「为什么需要验证层」
   的最好论据：**不变量只在被验证时才成立**。

## 9. 已知局限与后续工作

| 局限 | 说明 | 后续方向 |
| --- | --- | --- |
| 多实例验证是"单 JVM 内的多实例模拟" | 实验九用「两个组件实例 + 并发线程」覆盖了"共享数据库、各自持有组件"的多实例语义；**未覆盖**真实多进程/多主机、网络分区、负载均衡路由、进程崩溃恢复、数据库主从延迟 | 真实多节点环境下用混沌工具复测（见后续扩展方案 3.2）；其余不确定项逐条列在 `../基线文档/可靠性设计.md` 10.3 |
| 异步回调的发送仍在实例内 | 支付状态已落库，但**回调的发送**由处理"确认扣款"的实例用内存线程池完成；该实例在重试期间崩溃则该次回调丢失（由对账补偿兜住）。多实例下 `notifyUrl` 需指向负载均衡地址 | 若要求回调不丢，应改为持久化投递（事件表 + 消费者），与 3.2 一起考虑 |
| 缓存类配置变更最长 60 秒收敛 | 状态机规则与系统参数是每实例 60 秒 TTL 缓存，管理端改完在另一实例上可能最长 60 秒后生效 | 需要"即刻全局生效"则引入版本号或失效广播 |
| 长稳是"高密度压缩"而非小时级 | 实验十一把 6 秒当作长稳的压缩代理，覆盖"持续负载下是否稳定收敛"，**不覆盖**运行数小时后的缓慢退化（缓存膨胀、表膨胀、连接泄漏） | 真实长稳需要小时级运行与资源曲线采集（后续扩展方案 3.2 的剩余部分） |
| 混沌注入是白盒的 | 注入点在代码里预先选定（扣配额/展开任务/签收聚合），覆盖"事务中间故障"这一类；**未覆盖**进程被杀、网络分区、数据库不可用、磁盘满、时钟跳变等黑盒故障 | 引入进程级/网络级故障工具后补齐；其中"进程被杀"与多实例部分重合 |
| 检测延迟"先执行后抛"的语义边界 | 注入点若自身就是事务边界，"抛异常"会变成"调用方看到失败、实际已生效"（模糊失败/悬挂），本实验刻意不混用该形态 | 若要验证模糊失败语义，应单独设计（幂等查询 + 用户可重试的提示） |
| ~~补偿规则存在覆盖盲区~~ | **已修复（3.4）**：补 `(2, ALL_TASKS_TERMINAL → 4)` 规则并放开 `AUTO_COMPLETE/2` 闸门；原用例已翻转为回归守卫。遗留一个语义边界：**子任务全部取消时也会被补为已完成**，"全部取消→已退订"的区分依赖退款设计（`全取消补偿` 规则仍默认停用） | 明确退款/已退订与"全部取消"的关系后启用相应规则与条件 |
| 度量精度依赖时间列类型 | 检测延迟最初只能落在 0ms / 1000ms 两档，因为过程层时间列是秒精度 `DATETIME`；已把 `process_pending_task` 的时间列改为 `DATETIME(3)` 后得到 p50 81ms / p95 216ms。**时间列精度也是可观测性的一部分** | 其余过程层表（体检记录、迁移台账）仍是秒精度；需要更细的度量时按同样方式升级 |
| 数据库单一 | 部署目标为 MariaDB 12.1，已在其实测；MySQL 8 未实测（`mysql-connector-j` 与 SQL 语法均兼容，不是必需项） | 若需在 MySQL 8 部署，跑同一套实验复测即可（统一 `READ_COMMITTED` 后预期与 MariaDB 行为一致） |
| 支付为模拟实现 | `WechatPaySimulator` 为内存实现，重启丢失预支付单；这也是**唯一的本地状态依赖**（多实例下回调可能落到没有该预支付单的实例） | 接入真实支付、或把预支付单落到 `payment_record` 后重做实验二 |
| 修复动作之间的相互影响靠"逐对声明"消解 | 实验七对照 A 发现的组合异常已按方案一（组合守卫）+ 方案二（声明依赖 + 拓扑序 + 根因收窄）加固：异常从"一轮内收敛"变为**根本不产生**（实测订单未被误判、守卫零触发） | 但依赖关系仍需人工逐对写出；更远的方向是把不变量看作约束系统、由约束集合求解一致状态（方案三，见后续扩展方案 §2.5） |
| 缺长时间稳定性测试 | 未做数小时级的连续压测与对账命中率统计（实验十一是秒级压缩负载） | 小时级运行 + 资源曲线（内存/连接/表增长）采集 |
| ~~缺性能基线~~ | **已补**（实验十二）：热点行争用的 TPS/延迟与"单池 vs 分散多池"对比已量化；但口径是**进程内服务调用**，且为**单机**环境 | HTTP 全链路（JMeter/k6）与多机部署下的复测；分段池的"利用率损失"未实测（目前是推论） |
| 性能数据波动较大 | 单机开发环境（与 IDE/数据库同机），多轮运行 TPS 波动约 ±20%；**结论依赖的是比值（饱和/倍数），不是绝对值** | 独立压测机 + 固定资源配额后复测 |
| 台账写入为「尽力而为」 | 迁移台账写失败只告警，不阻塞业务 | 如需强审计，改为同事务强一致并接受写放大 |

> 完整的扩展方案（含工作量、论文价值与风险的评估）见 `../论文/后续扩展方案.md`。
