# 后续扩展方案（"雕花"清单）

> 定位：本项目已达成 **功能完成 + 正确性量化验证 + 第一档深度扩展**。
> 本文是**深度扩展**的设计方案，每项给出：现状 → 问题 → 方案 → 代码落点 → 工作量 → 论文价值 → 风险。
>
> **状态（2026-09-19 更新）**：**第一档（2.1–2.5）与第二档（3.1–3.4）全部实现并验证**
> （实验九~十二 + 3.4 补齐，共 30 个用例全绿）；第三档按约定只写进 future work，不实现。
> 其中 §2.5 已完成**方案一**（组合状态守卫）与**方案二**（声明依赖 + 拓扑序 + 根因收窄），
> 方案三（约束求解式修复）作为**展望**。
>
> 工作量以「1 人日 ≈ 半天可交付并可验证」估算；风险指"做错会伤到已有正确性"的程度。

## 0. 先回答：现在算完成了吗

| 维度 | 现状 |
| --- | --- |
| 功能 | 9 个业务模块，三端可用，91 个接口 |
| 机制 | 统一迁移出口（规则表 + CAS + 留痕）、幂等守卫（数据库唯一键）、配额台账精确回补、**规则驱动的补偿引擎**、**实时自愈双通道**、**7 类跨表不变量体检（含组合守卫）** |
| 验证 | **12 组实验**（含带对照的深化实验）+ **契约矩阵测试** + 1 个不变量用例，共 30 个用例；量化数据齐备 |
| 缺陷 | 实验与审计共暴露并修复 6 个真实缺陷（含 2 个只在并发下出现）；另有 1 项由实验七主动发现的组合异常，已诚实记录并列入后续工作 |

**结论**：第一轮实现达到"能交"；第一档深化后，异常恢复从"代码里的两个 if + 定时全表扫"
升级为"**可配置的补偿决策 + 双通道自愈 + 运行期持续验证**"，抽象在代码里有明确落点。

第一档已经消除的两处结构性偏薄：

1. ~~**补偿是硬编码的**~~ → 已下沉到 `process_reconcile_rule`，探测/决策/执行三层分离（见 2.1）；
2. ~~**验证是一次性的**~~ → 7 类不变量由体检任务周期求值（见 2.2），漂移检测从 10 分钟拉模式
   变为 5 秒级事件驱动（见 2.3）；实验发现的组合异常也已加守卫（见 2.5）。

## 1. 分档总览

| 档 | 编号 | 项目 | 工作量 | 论文价值 | 风险 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 一（建议做） | 2.1 | 补偿规则下沉到规则表 | 2–3 人日 | ★★★ | 中 | ✅ 已完成 |
| 一 | 2.2 | 不变量校验器：2 类 → 7 类跨表不变量 | 3–4 人日 | ★★★ | 低 | ✅ 已完成 |
| 一 | 2.3 | 实时自愈 + 定时兜底**双通道** | 3 人日 | ★★★ | 中 | ✅ 已完成 |
| 一 | 2.4 | 状态机契约矩阵测试（防旁路回归） | 2 人日 | ★★ | 低 | ✅ 已完成 |
| 一 | 2.5 | 修复动作的依赖与顺序约束（本轮实验新发现） | 1（方案一+二） | ★★★ | 中 | ✅ 方案一+二已完成 |
| 二（可做） | 3.1 | 多实例正确性：本地状态消除 + 幂等边界验证 | 3 人日 | ★★★ | 中 | ✅ 已完成 |
| 二 | 3.2 | 混沌注入实验 + 长稳对账度量 | 3 人日 | ★★★ | 低 | ✅ 已完成 |
| 二 | 3.3 | 性能基准（TPS/P99 + 热点行争用） | 2 人日 | ★★ | 低 | ✅ 已完成 |
| 二 | 3.4 | 补偿规则覆盖补齐（实验十一发现的盲区） | 0.5 人日 | ★★ | 低 | ✅ 已完成 |
| 三（写 future work） | 4.1 | 嵌套/层次化状态机（三级） | 4–5 人日 | ★★★ | 高 | 📄 仅文档 |
| 三 | 4.2 | 规则表升级为轻量规则引擎（守卫条件 + 副作用声明） | 4 人日 | ★★ | 中 | 📄 仅文档 |
| 三 | 4.3 | 人工介入通道（补偿升级） | 2 人日 | ★★ | 低 | 📄 仅文档 |
| 三 | 4.4 | 失败事件表 + 显式重试（死信语义） | 2 人日 | ★★ | 低 | 📄 仅文档 |
| 三 | 4.5 | 配额预占（下单占货，超时释放） | 2 人日 | ★ | 中 | 📄 仅文档 |
| 三 | 4.6 | 过程回放可视化（时间轴 + 状态图） | 2 人日 | ★★ | 低 | 📄 仅文档 |
| 三 | 4.7 | 批次追溯钩子（常温奶六个月保质期的真实缺口） | 1 人日 | ★★ | 低 | ✅ 已实现（2026-09-21） |

## 2. 第一档：建议做（论文直接受益）

> 全部已完成。下面每节保留设计方案原文（便于论文引用），并在开头补「实现落点与验证」。

### 2.1 补偿规则从硬编码下沉到规则表

> **✅ 已实现（2026-09-19）**
> - 落点：`process/reconcile/`（`ProcessReconcileEngine`、`ProcessReconcileCoordinator`、
>   `ProcessReconcileRule`、`ParentProcessProbe`、`ParentProcessAggregator`）、
>   表 `process_reconcile_rule`、业务侧 `OrderProcessProbe` / `OrderProcessAggregator`；
> - 原 `OrderInfoService.reconcileOrderAggregation` 已删除（避免两套判定口径并存）；
> - 验证：实验四（链路端到端）、实验六（规则启停 + 迁移闸门）；
> - 文档：`docs/基线文档/业务过程与可靠执行模型.md` §3.6、`可靠性设计.md` §7.2。
>
> 实现时补上的两处设计细节（原方案未写）：① 同一主体一轮只补偿一次（按规则 id 取首个命中），
> 避免多规则叠加导致一次补偿连跳两个状态；② 新增补偿动作若未在聚合器实现，**快速失败**而不是静默跳过。

**现状**（`OrderInfoServiceImpl.reconcileOrderAggregation`）：

```java
// 场景一：已支付但子任务已开始配送 —— 补齐"配送中"联动
if (deliveryTaskService.hasDispatchingTask(order.getId()) && self.markDeliveringIfPaid(order.getId())) { ... }
// 场景二：配送中但子任务全部到达终态 —— 补齐"已完成"聚合
if (self.completeOrderIfAllTasksDone(order.getId())) { ... }
```

**问题**：
- 补偿规则（父状态 + 子过程条件 → 动作）**硬编码**，与状态迁移规则表（数据驱动）形成不对称；
- 想增加第三类漂移（如"配送中 + 子任务全取消 → 应退订"）必须改代码、重新发布。

**方案**：新增规则表 + 探测器/决策/执行三层分离。

```sql
CREATE TABLE IF NOT EXISTS process_reconcile_rule (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '规则ID',
    name VARCHAR(64) NOT NULL COMMENT '规则名',
    parent_scene VARCHAR(30) NOT NULL COMMENT '父过程场景，如 ORDER',
    parent_status INT NOT NULL COMMENT '父过程需满足的状态，如 2-已支付',
    child_condition VARCHAR(40) NOT NULL COMMENT '子过程条件编码，如 HAS_DISPATCHING_TASK',
    action VARCHAR(30) NOT NULL COMMENT '补偿动作（复用 StateTransitions 动作码）',
    target_status INT NOT NULL COMMENT '补偿后的父状态',
    enabled TINYINT NOT NULL DEFAULT 1 COMMENT '是否启用（1-启用，0-停用）',
    description VARCHAR(255) COMMENT '规则说明',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_reconcile (parent_scene, parent_status, child_condition, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='过程补偿规则表';
```

种子（前两条 = 当前硬编码行为，第三条是"配置出来"的新能力，默认关闭）：

```sql
('联动丢失补偿', 'ORDER', 2, 'HAS_DISPATCHING_TASK', 'DELIVER',        3, 1, '已支付且存在配送中子任务 → 补偿为配送中'),
('聚合丢失补偿', 'ORDER', 3, 'ALL_TASKS_TERMINAL',  'AUTO_COMPLETE',  4, 1, '配送中且子任务全部终态 → 补偿为已完成'),
('全取消补偿',   'ORDER', 3, 'ALL_TASKS_CANCELLED', 'CANCEL',         5, 0, '配送中且子任务全部取消 → 补偿为已退订（默认关闭，需线下退款配合）'),
```

三层结构：

| 层 | 职责 | 实现 |
| --- | --- | --- |
| 探测器 | 按规则表里出现过的 `parent_status` 分组取候选父过程（每组 `LIMIT n`），逐条求值子过程条件 | `ProcessReconcileEngine.detect(scene, limit)` |
| 决策器 | 命中的规则交给谁执行（规则里已写明 action 与 target_status） | 规则表 |
| 执行器 | 复用 `ProcessTransitionExecutor.attempt`，成功后写迁移台账（`remark` 标注"由规则 X 补偿"） | 已有 |

**关键设计点**：
- 子过程条件做成**显式枚举 + 求值器**（`HAS_DISPATCHING_TASK` / `ALL_TASKS_TERMINAL` / `ALL_TASKS_CANCELLED`），新增条件需要改代码——这是**有意的边界**：条件求值涉及业务语义，不适合纯配置化，配置化的对象是"**用哪个条件、补偿到什么状态**"；
- 每条规则独立开关，默认只开"与现有行为等价"的两条 → **行为可灰度、可回滚**；
- 单轮上限 + 迁移台账留痕 → 补偿放大可追溯。

**论文价值**：把"异常恢复"从代码里的两个 `if` 提升为**与状态迁移对称的可配置机制**，可以写"检测（探测）/决策（规则）/执行（统一出口）三层分离"，并讨论"哪些该配置化、哪些不该"。

**风险**：规则配错会放大问题 → 靠默认关闭 + 限批 + 台账回溯控制。

---

### 2.2 不变量校验器：从 2 类漂移到 7 类跨表不变量

> **✅ 已实现（2026-09-19）**
> - 落点：`process/invariant/`（`ProcessInvariant`、`ProcessInvariantScanner`、`InvariantScanReport`）、
>   表 `process_invariant_violation`、7 个业务侧检查项（`module.product.invariant`、
>   `module.delivery.invariant` × 3、`module.order.invariant` × 3，含 §2.5 的组合守卫）、
>   `ProcessInvariantJob`；
> - 实现时的三处调整：① 接口签名由「批量修复返回条数」改为**逐条 `repair(violation)`**，
>   因为只有逐条才知道哪一条真的修好了、哪一条失败（体检记录要按条闭环）；
>   ② 记录表由「每次检出追加一行」改为**每个（不变量 × 主体）一行当前状态 + `reopen_count`**，
>   否则反复漂移会堆出多行，反而看不出当前是否一致；`reopen_count` 同时成为自愈有效性的度量；
>   ③ **修复判定改用每条违规的实际等级**，使"同一条约束下有的可自动修、有的只能告警"成为可能
>   （§2.5 的守卫依赖这一点）；执行顺序按编码固定以保证可复现；
> - 验证：实验七（对照 A 注入四类全修复 + 复检闭环；对照 B 资金项只告警；对照 C 等级细分）；
> - 文档：`业务过程与可靠执行模型.md` §3.8、`可靠性设计.md` §7.4。

**现状**：只有"父状态与子任务集合"这一类不一致被检测。其它跨表不变量（配额账实、签收与营养、任务与记录）**从未在运行期被检查过**。

**方案**：把不变量做成可注册的检查项，由 Job 周期执行"体检"。

```java
public interface ProcessInvariant {
    String code();                                   // 如 QUOTA_LEDGER_MISMATCH
    String description();
    InvariantSeverity severity();                    // 与修复策略绑定
    List<InvariantViolation> detect(int limit);      // 只探测，不改数据
    int autoRepair(List<InvariantViolation> v);      // 仅幂等、可逆的项支持自动修复
}
```

七类不变量（都不需要新表，全是既有表之间的约束；INV-7 是实验发现后补上的组合守卫）：

| 编号 | 不变量 | 检测口径 | 处置 |
| --- | --- | --- | --- |
| INV-1 | 配额账实一致 | `daily_quota.used_quota` 是否等于 `SUM(daily_quota_usage.boxes) WHERE deleted = 0`（同日期+品种） | 自动修：以台账为准重算 `used_quota` |
| INV-2 | 任务-记录一致 | 任务 `status=3`（已完成）但签收记录 `sign_status<>1` | 自动修：补签收记录 |
| INV-3 | 签收-营养一致 | 签收记录 `sign_status=1` 但无对应 `nutrition_intake` | 自动修：按签收记录补生成 |
| INV-4 | 订单-任务一致 | 任务数是否等于「订单品种数 × 配送天数」（按品种合并后） | 自动修：补生成缺失任务（幂等） |
| INV-5 | 父状态-子集合一致 | 现有两类漂移 | 自动修（并入 2.1 的引擎） |
| INV-6 | 支付一致 | 订单已支付但无成功流水 / 有成功流水但订单非已支付 | **只报警**：涉及资金，必须人工核 |
| INV-7 | 父过程终态一致（**组合守卫**，见 2.5） | 订单已完成/已退订但存在待配送或配送中的子任务 | 待配送自动作废（经业务取消出口）/ 配送中**只报警**（奶已出库在途） |

**关键设计点（这是最容易写错的地方）**：
- **口径要显式**：台账行回补后是**逻辑删除**（`deleted=1`），所以 INV-1 必须写 `deleted = 0`；否则会长期误报。这个口径本身就是可写进论文的一节"口径与假阳性"。
- **探测与修复分离**：探测只读、可随时跑；修复必须经统一出口（复用迁移出口/聚合出口/业务取消出口），保证留痕与幂等；
- **严重度分级**：`AUTO_REPAIR`（幂等可逆）/ `ALERT_ONLY`（涉资金、需人工）；等级可**细到每条违规**（INV-7 即"待配送可自动修、配送中只能告警"）；
- **执行顺序固定**：各项按编码排序执行，避免不可复现（INV-4 会新增子任务、INV-7 正是要发现它）。

**论文价值**：★★★ 最高。这正是本项目自己论证过的那句话——**"不变量只在被验证时才成立"**：实验里验证一次只是抽样，运行期周期校验才是持续保证。可以据此写"从一次性实验到运行期持续验证"。

**工作量**：1 张违规记录表（记录 code/violation/detected_at/handled_at，便于看板与统计）+ 7 个探测器 + 1 个 Job + 管理端只读页。

**风险**：低（以只读探测为主；自动修限定在幂等、可逆动作）。

---

### 2.3 实时自愈 + 定时兜底"双通道"（"实时推"的正确解法）

> **✅ 已实现（2026-09-19）**
> - 落点：`process/pending/`（`ProcessPendingTaskService`）、表 `process_pending_task`、
>   `ProcessPendingTaskJob`（每 5 秒消费）、6 处业务侧入队点（送出/签收/拒收/取消/缺货取消/退订联动作废）；
> - 实测：实时窗口 5014ms（轮询 5000 + 修复 14ms）vs 兜底 600000ms，缩短 119 倍。
>   值得写进论文的一点：**瓶颈是轮询间隔而不是修复**——「实时」的收益来自发现得早，不是修得快；
> - 实现时的两处细节：① 去重键含 `status`，否则逻辑删除的已处理行会阻塞同一业务键的新待办，
>   使其**静默入队失败**（详见 `可靠性设计.md` 第六节）；② 入队必须在子过程状态变更的同一事务内；
> - 验证：实验八（对照 A 窗口对比 / 对照 B 去重 / 对照 C 退避与放弃）；
> - 文档：`业务过程与可靠执行模型.md` §3.7、`可靠性设计.md` §7.3。

**现状**：`OrderProcessReconcileJob` 每 10 分钟全表扫两类订单。漂移的**不可检测窗口最长 10 分钟**，且全表扫随数据量增长而变慢。

**先说清楚"实时"到底解决什么**：它不是为了让界面更快，而是为了**把"系统处于错误状态而不自知"的窗口从分钟级压到秒级**。但自愈本身也会失败，所以实时通道**不能替代**兜底——两者必须是双通道。

**方案**：事务内落地待办（outbox 变体）+ 秒级消费者 + 定时兜底。

```sql
CREATE TABLE IF NOT EXISTS process_pending_task (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    scene VARCHAR(30) NOT NULL COMMENT '待补偿的过程场景，如 ORDER',
    entity_type VARCHAR(50) NOT NULL COMMENT '父过程表名',
    entity_id BIGINT NOT NULL COMMENT '父过程主键',
    biz_no VARCHAR(64) COMMENT '业务单号',
    trigger_action VARCHAR(30) NOT NULL COMMENT '触发动作（子过程终态动作），用于去重',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0-待处理，1-已处理，2-已放弃',
    retry_count INT NOT NULL DEFAULT 0 COMMENT '已重试次数',
    next_retry_time DATETIME COMMENT '下次可处理时间（指数退避）',
    last_error VARCHAR(255) COMMENT '最近一次失败原因',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_pending (scene, entity_id, trigger_action, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='过程自愈待办（实时通道）';
```

流程：

```text
子过程状态变更成功（同一事务）
        ↓ 写入一条待办（唯一键去重，已存在则忽略）
        ↓
秒级消费者（每 5 秒，LIMIT n）
        ↓ 调用聚合/补偿出口（attempt，天然幂等）
   ┌────┴────┐
成功         失败
  ↓            ↓
置已处理   retry_count++、指数退避，超 5 次置「已放弃」并告警
                ↓
        定时兜底（10 分钟）扫「已放弃 + 漂移候选」，最后防线
```

**关键设计点**：
- 写入点在**事务内**，保证"副作用发生 ⇒ 待办存在"（不会因为进程崩溃而丢待办）；
- 唯一键 `(scene, entity_id, trigger_action, status)` 防止同一触发动作堆积多条待办；
- 消费者用 `attempt` 语义 → 重复消费不产生额外状态变更；
- **超限即降级**：重试 5 次仍失败就交给兜底通道 + 告警，避免消费者被单条脏数据拖死（与现有 `MAX_AUTO_SIGN_BATCH` 同一思路）；
- 兜底通道保留，职责从"主责"降为"最后防线"。

**论文价值**：★★★ 可以直接写"实时自愈 + 定时兜底双通道"，并论证"**为什么不能只有实时通道**（自愈会失败）"和"**为什么不能只有兜底通道**（窗口太长）"。这是一个完整的设计论证，而不是功能堆砌。

**风险**：中。事务内多一次 insert 有写放大；待办表会成为新的热点，需按 `next_retry_time` 建索引并限批。

---

### 2.4 状态机契约矩阵测试（防旁路回归）

> **✅ 已实现（2026-09-19）**
> - 落点：`src/test/java/com/milk/order/contract/StateMachineContractTest.java`；
> - 实现时的调整：矩阵 A 由「遍历全部组合做真实迁移」改为**断言规则表覆盖全部可达组合**
>   （因为白名单语义下漏配一行 = 该迁移静默被禁止，这是覆盖矩阵能抓到的最有价值的一类问题）；
>   矩阵 C 由「台账条数」升级为**台账可回放**（逐步断言 from 等于回放到此刻的状态，无缝隙、无跳跃）；
> - 验证：3 个用例全绿（35 个组合零缺失、12 项安全关键迁移均禁止、回放 1→2→3→4）;
> - 已知未覆盖：D6（自调用导致 `@Transactional` 失效）仍无自动化守卫。

**现状**：D5（联动路径绕过统一出口导致台账空洞）是"逐条核对不变量"发现的，**没有自动化守卫**；D6（自调用导致事务失效）同样没有守卫。

**方案**：把"状态机契约"做成参数化测试矩阵，而不是零散用例。

- **矩阵 A（迁移合法性）**：对每个主体（订单 5 状态 × 5 动作、配送任务 4 状态 × 5 动作）遍历全部组合，
  断言"规则表允许 → 迁移成功且台账恰有 1 条生效记录"、"规则表禁止 → 被拒绝且状态不变"；
- **矩阵 B（留痕完整性）**：任何一次状态变更后，断言台账条数 == 变更次数（实验五的全场景版）；
- **矩阵 C（事务边界）**：断言所有标注 `@Transactional` 的 public 方法都**经 Spring 代理**调用（可用 `AopUtils`/`ApplicationContext` 校验自调用黑名单），把 D6 变成"不可能悄悄回归"。

**论文价值**：★★ 把"经验教训"变成"可执行的契约"，也是"验证层"从实验扩展到测试的体现。

**风险**：低。

### 2.5 修复动作的依赖与顺序约束

**来源**：这不是事先设计出来的方案，而是**实验七对照 A 主动发现了自己的一个缺口**——
这是"验证层"真正价值的又一次体现，值得单独成节。

**现象**：对照 A 同时注入 4 类互不相关的违规，各条不变量的修复全部成功（4 检出 / 4 修复 /
次轮复检 0），但最后一个观察项显示：**修复后仍有 1 条未完成任务，而订单已经是已完成状态**。

**根因**：修复动作之间缺少依赖与顺序——
`INV_ORDER_TASK` 补回一条缺失的任务（状态=待配送）可能发生在 `INV_ORDER_AGGREGATION`
把订单聚合为已完成之后，于是得到「已完成订单 + 待配送任务」这种组合可疑状态。
**当时的 6 条不变量都没有被违反**，因为没有任何一条覆盖「父过程已终态时不应存在非终态子过程」
这个组合约束。它说明：**单个机制正确 ≠ 机制组合正确**。

#### 方案一：补一条组合状态守卫 —— ✅ 已实现（2026-09-19）

> **实现落点**
> - 新不变量 `INV_PARENT_TERMINAL_CHILD_PENDING`（`module/order/invariant`）：
>   订单处于终态（4 已完成 / 5 已退订）却存在非终态子任务时检出；
> - **处置等级按具体情况下分**（同一条不变量内部允许不同等级）：
>   - 残余子任务「待配送」→ `AUTO_REPAIR`：经业务取消出口作废（含未签收记录作废、迁移台账留痕、
>     自愈待办入队），因此修复本身也留痕、也不绕过可靠性层；
>   - 残余子任务「配送中」→ `ALERT_ONLY`：奶已出库在途，自动作废等于把已送出的奶从记录里抹掉，
>     必须人工处理（与"缺货批量取消只取消待配送任务"的口径一致）；
> - 为支持上一条，体检框架做了一处小改进：**修复判定从「不变量级等级」改为「每条违规的实际等级」**
>   （未声明等级时回退到不变量默认等级，且回退方向是"不自动修"）；
> - 另将各项不变量的执行顺序**按编码排序固定**：顺序不稳定会让体检结果不可复现
>   （因为「补网格」会新增一条待配送子任务，而守卫正是要发现它）；
> - 验证：实验七对照 A（守卫在同轮内命中 1 条并作废，未完成任务数归零、次轮复检 0）
>   与新增对照 C（配送中只告警 / 待配送自动作废的等级细分）。

**效果与边界**：组合异常从"永远存在但无人知晓"变为**一轮体检内收敛**（实测：守卫命中 1 条、
当归零、次轮 0 检出）。**但要诚实说明**：这是"把问题变可见并收敛"，不是"消除根因"——
修复动作之间的**顺序依赖仍未声明**，仍可能出现"先补上、再作废"这种绕一圈才收敛的过程。
根因的消除需要下面的方案二/三。

#### 方案二：给修复动作声明显式依赖 —— ✅ 已实现（2026-09-19）

> **实现落点**
> - `ProcessInvariant.dependsOn()`：声明「本项必须在哪些编码之后执行」（默认无依赖）；
> - `ProcessInvariantScanner` 由"按编码字典序"改为**按声明依赖的拓扑序**执行，
>   同层内按编码升序打破平局（因此结果确定可复现）；依赖引用未注册编码或成环时**直接失败**，
>   不静默退化成任意顺序；`resolvedExecutionOrder()` 暴露当前顺序供测试与人工审阅；
> - 声明了两条依赖：`INV_ORDER_AGGREGATION → dependsOn(INV_ORDER_TASK)`
>   （父聚合依赖子网格已完整）、`INV_PARENT_TERMINAL_CHILD_PENDING → dependsOn(INV_ORDER_TASK,
>   INV_ORDER_AGGREGATION)`（组合守卫必须最后判最终子集合）；
> - **同时收窄了根因**：`INV_ORDER_TASK` 现在按订单状态分级——进行中（已支付/配送中）补网格，
>   已终态（已完成/已退订）**只告警、不补造历史**。原先不区分状态地补网格，
>   会给一个已结束的订单造出待配送任务，正是组合异常的产生源；
> - 验证：`InvariantDependencyOrderTest`（拓扑性 + 关键序对 + 确定性 + "不是字典序巧合"）
>   与实验七对照 A（**行为验证**：注入后若父聚合先跑，同一次体检就会把订单判成已完成再补回任务；
>   实测订单保持配送中、组合守卫 0 触发）、对照 D（终态缺口不补造 → 不产生组合异常）。
>
> **结论**：组合异常从"一轮内收敛"进一步变为**根本不产生**；组合守卫退化为"最后防线"
> （只在人工改库、进程异常等真异常下才会命中）。原先那段"先补上、再作废"的绕圈过程消失了。

**📄 展望**：不变量之间本就是一个**约束系统**。当前形态是"每条约束独立探测、独立修复"，
理想形态是**一致性问题求解**：给定约束集合与当前状态，推导出一个满足全部约束的修复计划
（而不是逐条试错）。这涉及约束求解、修复代价评估与最小改动集，已明显超出本科毕设范围，
作为"展望"中的深度方向：**从"逐条修复"到"约束求解式修复"**。

**论文价值**：★★★ 这是一个**自己发现自己的缺口**的案例，非常适合写进"验证层的价值"与
"已知局限/展望"两处；方案一已实现，方案三作为展望，链条完整：
*各机制分别正确 → 组合出现问题（自己发现）→ 先加固为可见可收敛 → 深度方向留给展望*。

**风险**：低（方案一已实现并验证）；方案二会引入顺序与并发的新问题，需与幂等性一起验证。

## 3. 第二档：可做（实现/实验章节加分）

### 3.1 多实例部署的正确性

> **✅ 已完成（2026-09-19）**
> - 完整审计表（每个组件的本地状态 / 后果 / 处置）与**未覆盖的不确定项**：
>   `docs/基线文档/可靠性设计.md` 第十节；
> - 修复落点：`wechat_pay_order` 表 + `WechatPaySimulator` 改为读写库存状态；
>   `InstanceIdentity`（进程实例标识，用于订单号/流水号）；
>   `DeliveryTaskServiceImpl.buildTaskNo`（任务号由业务键确定性生成）；
>   `ProcessInvariantScanner.insertOpen`（违规记录幂等插入）；
> - 验证：实验九（5 个对照，见 `docs/实验/实验设计与结果.md` §6.4）；
> - **审计比预想的多挖出两个问题**：原先只看到"定时任务无协调"和"预支付单在内存"两个障碍，
>   实际审计还发现**编号生成依赖实例本地计数器**——任务号撞号会被幂等守卫误判成"业务键已存在"，
>   导致**一条配送任务凭空消失**（静默丢数据，比报错更危险）；订单号撞号则表现为"下单失败"。
> - 结论：**不做选主也正确**（6 个定时任务的写路径全是 CAS/唯一键/幂等跳过，
>   "重复执行只浪费算力不产生错误"），选主属于算力优化而非正确性需求。

**原方案（保留，便于论文引用）**

**现状**：单实例部署。系统的并发安全依赖数据库（唯一键 + 条件更新 + 行锁），**原则上天然支持多实例**，但有两个真实障碍：

| 障碍 | 说明 | 方案 |
| --- | --- | --- |
| 定时任务无协调 | `@Scheduled` 在每个实例都会触发，对账/补偿/自动签收会**重复执行** | 先论证"所有 Job 都是幂等的"（`attempt` 语义），因此重复执行**只浪费不犯错**；再可选加轻量选主（`scheduler_lock` 表 + `SELECT ... FOR UPDATE` + 过期时间）降低浪费 |
| 预支付单在内存 | `WechatPaySimulator` 是内存态，多实例下回调可能落到**没有该预支付单**的实例 | 模拟器改为读写 `payment_record` 的待支付行（该行本就是预支付单），消除唯一的本地状态依赖 |

**论文价值**：★★★ 可以写一个很有力的小节：《为什么本设计能（也只有一个地方不能）天然支持多实例》——多实例的正确性来自"幂等 + 唯一键 + CAS"，而不是分布式协调；唯一的本地状态依赖（内存态模拟器）被明确指出并消除。

**风险**：中（需要把定时任务逐个再审计一遍幂等性）。

### 3.2 混沌注入实验 + 长稳对账度量

> **✅ 已完成（2026-09-19）**
> - 混沌注入器：`src/test/java/com/milk/order/chaos/`（测试域 AOP、固定种子可复现、默认撤防）；
>   **注入语义为"某一步已执行、外层事务尚未提交时失败"**，因此注入点必须位于事务内部
>   （若注入点自身就是事务边界，"抛异常"会变成"已提交但调用方看到失败"的模糊失败，属另一课题）；
> - 新增实验十（4 个对照）与实验十一（长稳度量 + 一个特征化测试），见
>   `docs/实验/实验设计与结果.md` §6.5–6.6；
> - 实测：中间失败零半截数据（状态/流水/任务/配额/台账全部回滚）；混沌风暴下 4 笔被打断的支付全部由
>   对账补回、零重复副作用；6 秒压缩长稳里 624 次人为漂移全部收敛
>   （兜底 543 / 体检 71 / 实时 10），检测延迟 p50 81ms、p95 216ms，最终零违规；
> - **顺带发现两个问题**：① 补偿规则存在覆盖盲区（`已支付 + 子任务全终态` 无规则，见 §3.4）；
>   ② 度量精度受时间列类型限制——秒精度 `DATETIME` 下延迟只剩 0ms/1000ms 两档，
>   已把 `process_pending_task` 的时间列改为 `DATETIME(3)`（**时间列精度也是可观测性的一部分**）；
> - **实现踩坑记录（值得写进论文的实践细节）**：切点最初写在接口上，Spring Boot 默认 CGLIB 代理下
>   方法签名的声明类型是实现类，导致**注入次数恒为 0**；改为 `service..*.method` 才生效。
>   这是"实验没反应"类问题的一个典型成因——**先确认注入真的发生了，再解读数据**。
> - 边界：长稳是"高密度压缩"而非小时级；混沌是白盒注入，未覆盖进程被杀/网络分区/磁盘满/时钟跳变。

**原方案（保留，便于论文引用）**

**方案**：
- **混沌注入**：用 AOP 在事务中间以概率抛异常 / 注入延迟 / 随机中断请求，运行混合负载后断言"系统最终收敛"（所有订单处于合法状态、所有不变量成立）；
- **长稳**：连续跑 2~24 小时混合负载，统计**对账命中数、补偿条数、检测延迟分布、台账冲突率、不变量违规数**——这些正是 2.2/2.3 之后才有的可观测量；
- 输出"自愈有效性"指标：漂移发生后被实时通道修复的比例 vs 被兜底通道修复的比例。

**论文价值**：★★★ 第 7 章从"正确性实验"扩成"**正确性 + 混沌 + 长稳**"三节，把"可靠性"从"正确"推进到"可度量"。

**风险**：低。

### 3.4 补偿规则覆盖补齐（🆕 实验十一发现的盲区）

> **✅ 已完成（2026-09-19）**，实现共三件事，并修正了方案里的一个误判：
> 1. 新增补偿规则「已支付全终态补偿」`(2, ALL_TASKS_TERMINAL → AUTO_COMPLETE, 4)`，默认启用；
> 2. **放开的是闸门 `ORDER/AUTO_COMPLETE/2`**（原方案误写为 `COMPLETE/2`——核对闸门种子后发现
>    补偿动作走的是 `AUTO_COMPLETE`，而 `COMPLETE/2` 是**管理端手动完成**的闸门，必须保持禁止。
>    "配了规则 ≠ 配对了规则"在这里多了一层：**连"该放开哪条闸门"都要先核对动作码**）；
> 3. 聚合出口 `completeOrderIfAllTasksDone` 从"仅配送中"放开为"已支付/配送中"，
>    CAS 以实际父状态为条件——同一出口服务正常路径与补偿路径，幂等留痕不变。
> 4. 实验十一的特征化用例已翻转为回归守卫：命中规则、补偿生效 1 次、订单到已完成、体检零检出。
>
> **实现时显式确认过的两个安全边界**：① `ALL_TASKS_TERMINAL` 含"至少一条任务"守卫，
> 不会把"已支付 + 零任务"（应付了钱没生成任务，应由 INV_ORDER_TASK 补生成）误补为已完成；
> ② 子任务**全部取消**时也会被本规则补为已完成（与状态 3 的现有语义一致），
> "全部取消 → 已退订"的区分依赖退款设计（`全取消补偿` 规则仍默认停用）。
>
> 验证：`mvn clean test` → 30 个用例全绿。

**现象（保留，便于论文引用）**：实验十一的特征化测试构造了「订单已支付(2) + 子任务已全部签收(3)」（配送联动与聚合写入同时丢失），
结果既没有规则命中、也不被任何不变量判为违规——订单静默停在"已支付"，而所有配送都已完成。

**根因**：规则表有 `(2, HAS_DISPATCHING_TASK → 3)` 与 `(3, ALL_TASKS_TERMINAL → 4)`，
**缺 `(2, ALL_TASKS_TERMINAL → 4)`**。

**论文价值**：★★ 与 2.5 同类——**规则表的覆盖面必须被主动审视**，而这次是长稳实验把它暴露出来的。

**风险**：低（已实现并验证；业务上"已支付订单在全部配送终态后转已完成"已确认合理）。

### 3.3 性能基准（TPS/P99 + 热点行争用）

> **✅ 已完成（2026-09-19，实验十二）**，实现方式与原方案的差异（及原因）：
> - **进程内基准代替 JMeter/k6**：压 `DailyQuotaService.deduct`（`Experiment12QuotaHotspotBenchmarkTest`，
>   `mvn test` 可复现）。原因：要单独暴露"行锁串行化"这一个因素，掺入 HTTP/序列化只会稀释结论；
>   且实验数据照常归档到 `target/experiment-reports/`。HTTP 全链路冒烟仍保留在
>   `docs/测试/系统测试用例.md` §4.2（未测）；
> - **"分段池"用多品种池做等价代理**：配额唯一键是 `(quota_date, product_id)`，没有班级这一维；
>   两者同为"把热点分散到多行"，行锁争用形态相同。真按班级分段需改配额维度与分配算法（后续工作）；
> - **实测**（单机，波动 ±20%，结论依赖比值）：热点单池 32 线程吞吐 **672 TPS，低于 8 线程的 759**（0.89×，
>   已饱和），p50 却涨到单线程的 9.8×；分散到 32 池吞吐 ×4（2670 TPS）；8 池 → 32 池仅 0.99×（锁外瓶颈）；
>   约 10600 次并发扣减**零失败、零超卖、账实一致**；
> - **修正了一个直觉**："单线程 TPS = 吞吐上限"是错的——单线程 TPS 含锁外开销，多线程可把锁外开销
>   与锁内临界区流水重叠（实测热点并发吞吐 ≈ 单线程的 3.2×，但远低于 32× 线性）；
> - **引出的权衡**：分段池解决热点但各段不能互相借用余量（利用率下降）→ **一致性-吞吐-利用率**三方权衡；
>   分析见 `docs/基线文档/可靠性设计.md` §12。

**原方案（保留，便于论文引用）**

**方案**：JMeter/k6 压测下单、支付、签收；重点测**同一配额池的热点争用**。

预期结论（有价值）：行锁读把同一池子的扣减**串行化**，吞吐上限 ≈ `1 / 单次事务时长`；因此热点品种会成为瓶颈。
由此自然引出对比实验：**单池 vs 按班级/批次分段池** 的吞吐差异 → 既是性能结论，也是"一致性 vs 吞吐"的权衡讨论。

**论文价值**：★★ 补上"性能测试"这一常被问到的环节。

**风险**：低。

## 4. 第三档：写进 future work 即可

> 按约定**本轮不实现**（除 4.6 可视化为可选演示增强；**4.7 已于 2026-09-21 实现**——它是合规义务，
> 与 4.1–4.5 的"表达完整性/工程取舍"不同类）。下面各项的价值在于「写清楚边界与代价」，
> 而不是做出来——尤其是 4.1 与 4.2，论文里讲清「为什么刻意不做」比做出来更有说服力。

### 4.1 嵌套/层次化状态机（三级）

**现状**：两级——订单（父）/ 配送任务（子），另有"签收记录"作为**不纳入规则表**的挂载子状态（判定标准：是否可被外部独立发起）。

**可以升级为三级**：把 `delivery_record` 提升为正式的第三级状态机并纳入规则表（需补 3 条种子规则）。

**我的建议：暂不做，写进 future work。** 理由：
- 收益主要是**表达层面的完整性**，而不是解决新的问题；
- 成本与风险不成比例：第三级纳入规则表后，"任务签收"与"记录签收"会形成**两份描述同一件事的规则**，管理员关掉一个忘了另一个就自相矛盾（这正是当初 D5 与本设计的耦合点）；
- 论文里**把它作为一个设计决策讨论反而更有价值**：写清"哪一级该纳入规则表"的判定标准（可被外部独立发起），比"全都能配"更专业。

若确实要做，正确做法是**父子联动规则**：规则表增加 `parent_scene/parent_action` 列，用"级联规则"表达"任务签收 ⇒ 记录签收"，而不是并列两份独立规则。

### 4.2 规则表升级为轻量规则引擎（守卫条件 + 副作用声明）

**现状**：`state_transition_rule` 是**布尔白名单**（scene/action/from_status → allowed），没有"条件"维度。

**可扩展**：增加 `guard_expr`（守卫：金额上限、时间窗、角色、业务开关）与 `effect`（副作用声明：写台账/发通知/扣资源）。

**建议**：作为 future work 并在论文里明确"**当前是白名单而非规则引擎**"这一事实边界（诚实反而加分），同时论证"为什么毕设阶段刻意不做条件维度"——它会引入表达式求值的安全性与可测试性问题，收益与复杂度不匹配。

### 4.3 人工介入通道（补偿升级）

自动补偿失败 N 次后，把主体标记为「需人工处理」，在管理端形成待办列表（可指派、可备注、可关闭）。
**价值**：★★ 这是"异常处理分类学"的最后一环：检测 → 决策 → 执行 → **升级人工**。当前链路止于"重试"。工作量小（1 张表 + 1 个页面），性价比高，也可升级进第一档。

### 4.4 失败事件表 + 显式重试（死信语义）

现状：失败即抛异常，依赖上游隐式重试（微信重试、定时任务下一轮），**重试过程不可观测**。
可改为显式"失败事件表 + 指数退避 + 死信状态"，与 2.3 的待办表可合并设计。

### 4.5 配额预占（下单占货）

现状：**支付成功时才扣配额**，因此存在"付了钱才发现当日配额不足"的体验问题（会产生一次失败回调 + 订单保持待支付）。
可改为下单时预占、超时释放。这是**业务侧**改进，与可靠性机制正交，工作量小但要处理"预占泄漏"（泄漏本身就是新的异常分支，需要新的兜底任务）。

### 4.6 过程回放可视化

把 `process_transition_log` 做成"单订单时间轴 + 状态变迁图"，管理端可点开任意订单看它的完整经历（含补偿记录）。
**价值**：★★ 论文配图与答辩演示效果极好，且是"可观测性"的直观体现。工作量小（前端为主，后端接口已有）。

### 4.7 批次追溯钩子（🆕 2026-09-21 登记；✅ 2026-09-21 已实现）

> **✅ 已实现（2026-09-21）**
> - 落点：`product_batch` 表（批号唯一 `uk_batch_no`）、`daily_quota.batch_no` 可选标注、
>   `module/product/service/impl/ProductBatchServiceImpl`（维护 + 召回反查）、
>   `ProductController` 的 `/api/product/batch/**` 五个接口、管理端「奶品管理 → 批次追溯」页
>   （`school-ui/src/views/product/components/BatchTab.vue`，含配额页的批次标注下拉）；
> - 反查链（一次请求完成三跳）：批号 → 配额池（日期×品种）→ 扣减台账 `daily_quota_usage`
>   → 订单/学生/班级 → 配送任务与签收状态，按配送日倒序输出"召回工作清单"；
> - 实现时的三处判断（原方案未写）：
>   ① 批号**不要求先建档**——反查以配额池上的标注为入口，事故当场可直接用批号查，
>      档案只提供生产/到货日期；② 设定配额时若批号已建档且**品种不一致**则拒绝，
>      避免批号串到别的品种池子上给出串味名单；③ 批量取数先用 (日期 in, 品种 in) 再按"池子键"
>      在内存精确过滤——(日期,品种) 的 OR 组合写成 in/in 会命中 9/1 的另一品种（笛卡尔式误命中）；
> - **实现时确认的边界**：学期套餐不经配额池，故反查**只覆盖单日零散订购**；
>   结转使"池子日期"可能早于"配送日"（9/1 的池子被 9/2 的订单扣走），故两者在结果里分列；
> - 未做（原方案的明确排除不变）：全量批次库存（入库/出库/效期预警）与"按批次生产日期动态结转"；
> - 文档：`研发规范/项目开发规范.md` §5.2、`基线文档/接口文档.md` §4、
>   `变更记录/2026-09-21-批次追溯钩子.md`；
> - 顺带做的边界说明：结转窗口仍是常量 `SHELF_DAYS=3`，**未参数化**（原方案"顺带做"一项未执行，
>   避免与本次改动混在一起；单独做只是搬家一个魔法数字）。


> **载体升级（2026-09-21 晚，随仓库方案立项）**：配送站确认为学校自己的后，仓库余量台账立项
> （`../设计方案/2026-09-21-仓库余量与出库边界-设计方案.md`），台账 IN 行自带 `batch_no`（可选）。
> 上方"已实现"条目的反查链仍有效（配额池标注入口）；仓库台账上线后，到货登记的 batch_no 将同时
> 落入 `warehouse_ledger`，批次-出入库-任务的追溯在台账内进一步闭合，反查链可逐步迁移到台账侧。
>
> **登记原因**：业务确认本项目配送的奶为常温奶，**实际保质期六个月**（非系统建模的 3 天）。
> 经评审**不改参数**——配额池建模的是"供货计划"而非奶的实体（详见
> `../设计方案/2026-09-21-保质期语义校正与批次追溯-设计方案.md`，语义已校正为"计划顺延窗口"），
> 真实缺口是**批次追溯**（批次召回、监管检查），登记于此作为持久化拓展项。
> **TODO 是缓冲清单可整份删除，本文是这些拓展的长期归属——拓展项同时记录在两处，删 TODO 不丢。**

**现状与缺口**：无批号维度。真出批次事故（灭菌故障、包材破损 → 同批数千盒不合格）时，
系统回答不了"这 500 盒送给了哪些孩子"——这是六个月常温奶供应链里最真实的合规需求
（食品安全法要求批号可追溯），且短保奶不存在（单盒变质即损耗），属于常温奶特有的建模缺口。

**方案（钩子，不是全量库存系统）**：

| 项 | 设计 |
| --- | --- |
| 数据 | `product_batch`（批号 uk、productId、生产日期、到货日期、状态）+ `daily_quota.batch_no`（**可选**外键：设置配额池时可关联当日到货批次，不强制） |
| 日常业务 | **不依赖它**：备货按计划、任务展开/扣减/台账全部照旧，批次只是池子上的可选标注 |
| 召回查询 | 三个反查：批号 → 关联池子（日期×品种）→ 台账（该池扣了哪些订单）→ 订单/任务/学生。**无新状态机、无新迁移** |
| 触发场景 | 批次召回、监管检查——低频但属合规义务 |
| 工作量 | 约 1 人日（建表 + 设置接口可选传 batch_no + 一个反查接口） |
| 论文价值 | ★★ "为业务决策建模，不为物理现实建模"的实证：说清池子是计划不是奶、批次属奶站责任域、钩子已预留——**能说清刻意不建什么、为什么，比建了什么更能证明建模能力** |
| 风险 | 低（不触碰既有扣减/台账/网格路径） |

**顺带做（做钩子时同批）**：把结转窗口从常量 `SHELF_DAYS` 提升为 `sys_config` 参数，语义名"计划顺延窗口"
（参数值仍为 3，行为不变；参数化后不同合同约定可调）。单独做它只是搬家一个魔法数字，不值得单独一轮。

**明确排除**：全量批次库存系统（入库/出库/效期预警）——属奶站 ERP 职责域，钩子 + 反查已满足召回需求；
结转逻辑改为按批次生产日期动态计算——会让计划模型依赖它不拥有的数据，把两个模型焊死。

## 5. 推荐组合

**方案 A（"深度三件套"，约 7–10 人日）**：2.1 + 2.2 + 2.3 —— ✅ **已全部完成**
三者互相咬合，形成一条完整论证链：

> 补偿规则可配置（决策层数据驱动）→ 不变量持续验证（发现问题）→ 实时自愈 + 定时兜底（恢复问题）

配套 2.4（契约矩阵）做回归守卫、2.5（组合守卫）补齐"机制组合"这一层，
论文可以新增一节"**从一次性验证到运行期持续自愈**"。

**方案 B（A + 系统级，约 12–15 人日）**：A + 3.1 + 3.2（+ 3.3）
再加多实例、混沌/长稳与性能基线，把"系统级可靠性"讲圆；
第 7 章可扩为"正确性 + 混沌 + 长稳 + 性能"。**状态：待定**。

**方案 C（最小，已不需要）**：只做 2.3 + 4.6 —— 已被方案 A 覆盖，仅保留 4.6（过程回放可视化）
作为可选演示增强。

**当前进行的组合（方案 B）**：A + 3.1 + 3.2 + 3.3，顺序为
3.1（多实例正确性，同时暴露唯一的本地状态依赖 `WechatPaySimulator` 内存态预支付单）
→ 3.2（混沌注入 + 长稳对账度量）→ 3.3（性能基准：TPS / P99，已知热点是同一配额池的行锁串行化）。

## 6. 与论文结构的对应

| 扩展项 | 应写入论文的位置 | 状态 |
| --- | --- | --- |
| 2.1 补偿规则下沉 | 第 5 章「异常恢复机制设计」新增小节：补偿规则的探测/决策/执行分层 | ✅ 有实测（实验六） |
| 2.2 不变量校验器 | 第 5 章 + 第 7 章：从一次实验到运行期持续验证 | ✅ 有实测（实验七） |
| 2.3 双通道自愈 | 第 5 章核心小节 + 第 7 章实验（自愈有效性度量） | ✅ 有实测（实验八） |
| 2.4 契约矩阵测试 | 第 7 章「测试与实验」 | ✅ 3 个用例 |
| 2.5 修复的依赖与顺序 | 第 7 章「实验发现」+ 第 8 章「局限与展望」（**自己发现自己的缺口**） | ✅ 方案一+二已实现（有实测）；方案三为展望 |
| 3.1 多实例 | 第 6 章「系统实现」+ 第 7 章实验（实验九）+ 第 8 章「局限与展望」 | ✅ 已实现（有实测） |
| 3.2 混沌/长稳 | 第 7 章新增两节 | ✅ 已实现（实验十、十一） |
| 3.4 规则覆盖补齐 | 第 7 章「实验发现」+ 第 5 章补偿规则小节 | ✅ 已实现（盲区已补齐，用例翻转为回归守卫） |
| 3.3 性能 | 第 7 章性能小节 | ✅ 已实现（实验十二） |
| 4.x | 第 8 章「总结与展望」逐条对应 | 📄 仅文档 |

> 若时间只够再做一项：**优先 2.5（修复的依赖与顺序）**——它是本轮实验自己发现的问题，
> 写进论文是最有说服力的一类材料：**持续验证能发现设计者没想到的组合问题**。
