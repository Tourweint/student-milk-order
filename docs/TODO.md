# 下阶段待办（缓冲清单）

> **这是什么**：本阶段之后"已明确要做但未做"与"待业务拍板"的事项汇总。它不是设计文档——
> 每条最终应落到对应的基线文档 / `变更记录/`，落完即从本文移除。**本文随时可整份删除。**
> **为什么要有它**：让接手的人或其他 Agent 不必靠对话上下文就能知道"下一步该干什么、哪些是刻意没做"。

最后更新：2026-09-21（第三轮五项全部实现并留档：下单日期校验、批次追溯、未送达申报、家长当日豁免、过敏软警示；G 段已清空）。

---

## A. 家长端「剩余待配送盒数」

| # | 事项 | 类型 | 触发条件 / 备注 |
|---|---|---|---|
| A1 | **一个家长多个孩子：切换孩子** | 新功能 | 当前 `sys_user.student_id` 是单值；需改账号模型 + 接口契约 + 小程序 UI。原文档 §4.3 |
| A2 | 首页计数的短 TTL 缓存（≈30s，进程内） | 可选优化 | 用户已明确"不优化也可以"。若做：只能是加速层（TTL 到期/丢失必回源、不得参与写路径），并须在 `可靠性设计.md` 记录为**显式例外** |

## B. 配送日平移与拒收补送（本轮已实现，遗留）

| # | 事项 | 备注 |
|---|---|---|
| B1 | 补送任务再被拒收时的**人工入口 / 告警落点** | 当前规则：只对原始任务自动补一次；补送任务再被拒收不自动再补。原方案 Q7 |
| B2 | 拒收原因字典扩充 | 当前 `DAMAGED / SOUR / WRONG_PRODUCT / SHORTAGE / OTHER`；扩充需同步 `schema.sql` 注释与前端下拉 |
| B3 | ~~假期日历 `holiday_calendar` + 跨学期平移~~ | 自动节假日识别**已定不做**；**周末停送与调休例外已实现**（`delivery_exception` 手动例外表 + `delivery.weekend.stop` 开关 + 一键日历重排，规则见 `研发规范/项目开发规范.md` §5.4.2、机制见 `基线文档/可靠性设计.md` §十四，2026-09-20 落地）。**仅剩跨学期平移**待定 |
| B4 | 平移的「按日期区间一键平移」入口 | 当前按勾选任务平移；**区间入口已由 B3 的「配送日历重排」部分覆盖**（按日期区间自动判定周末/停送日并重排），手工指定区间的任意平移仍缺 |
| B5 | ~~补送日改为"下一配送日"~~ | 已随 B3 简化：无自动节假日后无需"下一配送日"概念，**补送仍为原任务次日**（见 `基线文档/可靠性设计.md` §十三.3） |

## C. 已记录的其它边界（原方案 §5，本期不做）

> **拓展的长期归属说明（2026-09-21）**：本文是缓冲清单，**可随时整份删除**；
> 下列各项中已登入 `docs/论文/后续扩展方案.md` 的（如批次追溯钩子 §4.7），以那裡为长期归属，
> 删本文不丢。

| # | 事项 | 说明 |
|---|---|---|
| C1 | 部分破损按整盒补 | `quantity` 为整数盒，不支持半盒 |
| C2 | 家长要求退款而非补送 | **已解决（2026-09-21 落地）**：退款域已实现（按期次退款、退订联动补台账、毕业清算，实验十五 7/7 通过），见 `docs/变更记录/2026-09-21-退款与毕业清算.md` |
| C3 | 学生转班 / 转学 | 未完成任务的 `class_id` 跟着走还是作废？影响数据范围与配送分组（**毕业清算已随退款域落地**，见 `变更记录/2026-09-21-退款与毕业清算.md`；转班仍待定） |
| C4 | 错签反向 | 已签收需回滚（营养摄入已生成）→ `ALERT_ONLY` + 人工，不做自动回滚 |
| C5 | 换品种 | 建议走"原任务取消 + 在目标品种任务上合并/新建"，复用拒收补送的落账方式 |
| C6 | 学期中加量 / 减量 | 加量=再展开任务 + 扣配额；减量=取消未配送任务 + 台账回补 |
| C7 | 跨学期平移的配额预留 | 下学期初排产要把上学期尾巴算进去；系统需能查询"下 N 个配送日预计配送量" |
| C8 | **保质期六个月（2026-09-21 评审定）** | **参数不改**：配额池建模的是供货计划而非奶的实体，"3 天"语义已校正为"计划顺延窗口"（纯注释/文档变更，零行为变化）。真缺口 = 批次追溯，**已于 2026-09-21 实现**（`product_batch` + `daily_quota.batch_no` + 召回反查，见 `论文/后续扩展方案.md` §4.7 与 `变更记录/2026-09-21-批次追溯钩子.md`）。详见 `docs/设计方案/2026-09-21-保质期语义校正与批次追溯-设计方案.md` |

## G. 下一轮待实施（2026-09-21 已定口径，等排期）

> 本轮定下的两项已全部实现并留档（见下方「已完成」），G 段当前为空。
> 后续若还有"已定口径待实施"的事项列在此处。

> 已完成（2026-09-21，第三轮）：**下单可选过去日期**（服务端 + 两端前端，见 `变更记录/2026-09-21-下单配送开始日期不得早于今天.md`）、
> **批次追溯钩子**（实验十六，3 用例，`变更记录/2026-09-21-批次追溯钩子.md`）、
> **奶站当日未送达申报**（实验十七，3 用例，`变更记录/2026-09-21-奶站当日未送达申报.md`；机制见 `可靠性设计.md` §十六）、
> **家长端当日豁免**（实验十八，3 用例，`变更记录/2026-09-21-家长端当日豁免.md`；机制见 `可靠性设计.md` §十七）、
> **过敏/禁忌软警示**（实验十九，3 用例，`变更记录/2026-09-21-过敏与禁忌软警示.md`；规则见 `项目开发规范.md` §5.8）。
> 五项合计实测：12 个新用例全绿 + `mvn clean compile`/`npm run build` 通过；
> 开发库与实验库均已应用增量脚本，**全量 `mvn test` 未跑**（待实验库整库重建或按需补跑）。

## D. 验证与工程（本轮未跑）

| # | 事项 | 说明 |
|---|---|---|
| D3 | 若做 A2 短缓存，须在 `可靠性设计.md` 记录例外边界 | TTL、回源、多实例允许不一致、不得参与写路径 |

## E. 绘图（2026-09-21 工具切换遗留）

| # | 事项 | 备注 |
|---|---|---|
| E1 | ~~用例图、类图迁移 drawio~~ | **已完成**（2026-09-21）：三图全部产出 `.drawio`（部署图 / 用例图 / 类图），旧 `.puml` 留档对照，确认后可删；见 `基线文档/UML/README.md` |

## F. 退款与毕业清算（2026-09-21 落地，遗留项）

> 管理端与小程序界面均已实现（见 `变更记录/2026-09-21-退款与毕业清算.md` §2），以下为刻意未做项。

| # | 事项 | 备注 |
|---|---|---|
| F3 | 退款单侧取消入口（`CANCEL 1→5`） | 状态码与规则种子已就位（`REFUND/AUDIT|REJECT|EXECUTE/5` 均为 allowed=0），需要时补动作常量 + 规则行 + 契约矩阵登记；当前"申请后想少退"的处理是拒绝后重新申请 |
| F4 | 真实微信退款（T+1 / 回调 / 原路退回） | 需新增 `RefundReconcileJob` + 查单方法，`2→3` 改由回调驱动（与 `PaymentReconcileJob` 同构）；见 `可靠性设计.md` §十五.10 |

> 已完成（2026-09-20）：实验库已按最新 schema 重建；新增实验十三（4 用例）与
> `TaskCompensationInvariantContractTest`（1 用例），全量 **35 用例通过**——过程中暴露并修复缺陷 D7
> （摊平重复计入补送量）。详见 `实验/实验设计与结果.md` §6.9 与 §7。
>
> 已完成（2026-09-21）：**退款与毕业清算**（C2 解决、C3 的毕业侧解决）落地：退款域（申请/审核/执行、
> 退订联动补台账、毕业清算）+ 2 条 `ALERT_ONLY` 不变量 + 契约矩阵登记（规则总数 35 → 50）；
> 新增实验十五（7 用例）与退款域 HTTP 契约测试（5 用例），全量 **50 用例通过**；
> 管理端「退款管理」页与学生页「毕业清算」、
> 小程序「申请退款 / 我的退款单」均已完成（`npm run build` 通过）。详见
> `设计方案/2026-09-21-退款与毕业清算-设计方案.md`、`变更记录/2026-09-21-退款与毕业清算.md`、
> `可靠性设计.md` §十五、`项目开发规范.md` §5.7、`接口文档.md` §10、`实验/实验设计与结果.md` §6.11。
>
> 已完成（2026-09-20，第二轮）：**周末停送与调休例外**（B3 主体、B5 收口）落地，
> 新增 `delivery_exception` 表 + `delivery.weekend.stop` 开关 + `POST /api/delivery/task/calendar-rebalance`
> 与例外维护接口 + 管理端「配送日历」页；新增实验十四（3 用例），全量 **38 用例通过**。
> 详见 `设计方案/2026-09-20-周末停送与调休例外-设计方案.md`（设计取舍与实现纪要）、
> `变更记录/2026-09-20-周末停送与调休例外.md`（实现与实测数据）、`研发规范/项目开发规范.md` §5.4.2（业务规则）、
> `基线文档/可靠性设计.md` §十四（可靠性约束）、`实验/实验设计与结果.md` §6.10（实验十四）。

---

## 完成时的处置

- 做完一条：从本文删除，并在对应基线文档 / 新增 `变更记录/` 里留下结论与依据。
- 全部清空或阶段性结束：**直接删除本文**（它是缓冲，不是长期文档）。
